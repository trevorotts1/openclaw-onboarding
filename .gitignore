30-blackceo-voice-call-plugin-ARCHIVED/

__pycache__/
*.pyc

graphify-out/
# graphify (knowledge-graph map) — generated output, never committed

# PRD 1.8: finished migration artifact — deleted from repo, excluded from future tracking
projects/gemini-migration/

# PRD 3.3: repo hygiene — runtime/scratch dirs and backup files never belong in version control
tmp/
.openclaw/
*.TRUNCATED-BACKUP

# AF4: legacy-retirement-triggered is a runtime sentinel written by fleet_manifest.py
# when the retirement trigger fires. The sentinel is ephemeral (proof of trigger fire).
# fleet-manifest.json (the persistent per-box loaded-state tracker) IS committed.
legacy-retirement-triggered
