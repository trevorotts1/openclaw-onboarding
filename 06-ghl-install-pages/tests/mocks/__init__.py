# mocks — MOCK GHL + MOCK Gmail + MOCK secret store for the GHL email-2FA
# auth-fallback test suite. These NEVER touch a real GHL account or real Gmail;
# all I/O is in-memory so there is ZERO lockout risk.
