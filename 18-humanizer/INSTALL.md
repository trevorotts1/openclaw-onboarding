# Install - Humanizer

## Prerequisite
TYP must be completed first.

## Install steps
1. Create target folder:
   `mkdir -p ~/.openclaw/skills/humanizer`
2. Copy upstream skill files into target:
   `cp -R ./upstream-original/. ~/.openclaw/skills/humanizer/`
3. Verify:
   - `~/.openclaw/skills/humanizer/SKILL.md` exists
4. Apply only relevant core file updates from CORE_UPDATES.md
5. Do not modify non-relevant core files
