# Install - youtube-watcher

1. Create target folder:
   `mkdir -p ~/.openclaw/skills/youtube-watcher`
2. Copy files from `upstream-original/` into target.
3. Verify `~/.openclaw/skills/youtube-watcher/SKILL.md` exists.
