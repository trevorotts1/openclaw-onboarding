# Install - YouTube Watcher

## Prerequisite
TYP must be completed first.

## Install steps
1. Create target folder:
   `mkdir -p ~/.openclaw/skills/youtube-watcher`
2. Copy upstream skill files into target:
   `cp -R ./upstream-original/. ~/.openclaw/skills/youtube-watcher/`
3. Verify:
   - `~/.openclaw/skills/youtube-watcher/SKILL.md` exists
4. Apply only relevant core file updates from CORE_UPDATES.md
5. Do not modify non-relevant core files
