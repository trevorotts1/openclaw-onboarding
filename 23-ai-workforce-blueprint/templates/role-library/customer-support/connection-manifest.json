{
  "_comment": "Connection-manifest for the Customer Support department. verify-wiring.sh reads this and checks each required cfg_key.",
  "dept": "customer-support",
  "version": "1.0.0",
  "connection_points": [
    {
      "name": "ghl-api-key",
      "description": "GoHighLevel API key for the Customer Support department to manage conversations, support tickets, and follow-up automations.",
      "cfg_key": "env.vars.GHL_API_KEY",
      "required": true
    },
    {
      "name": "ghl-location-id",
      "description": "GHL location ID for the Customer Support sub-account.",
      "cfg_key": "env.vars.GHL_LOCATION_ID",
      "required": true
    }
  ]
}
