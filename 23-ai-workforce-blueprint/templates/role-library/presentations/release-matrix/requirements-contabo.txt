# PRES-043 Contabo Docker profile — exact pip pins (upgrade policy: pins.yaml)
# Identical set to the Hostinger profile today; kept as a SEPARATE file so a
# future profile divergence is a visible diff, not a shared-file edit.
reportlab==4.4.10
python-pptx==1.0.2
pypdf==6.7.5
pytesseract==0.3.13
Pillow==11.3.0
pytest==8.3.4
