# conftest.py — rootdir-level pytest config for 44-convert-and-flow-operator tests.
# No __init__.py so pytest imports each file as a root module, avoiding the
# 'tests' package collision with tools/engine/tests/__init__.py.
