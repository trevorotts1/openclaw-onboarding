# Signature reference set (placeholder)

Ships EMPTY. Default image mode is `none` (pure text-to-image; the look is carried entirely by the Signature Grade Block). Trevor loads the curated `signature_set` here later (PRD Open Decision O6); until then `reference_images.mode` stays `none`.
