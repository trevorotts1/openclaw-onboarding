<!-- BAKED PROMPT ASSET | stage 35-top-39 | subsystem facebook-ads
     source record: source/airtable-prompts/07-top-39-selected-in-each-ad-set.md
     provider-agnostic: resolved by the client's own TIER model at runtime; ZERO Anthropic ids.
     intake tokens -> {{intake.<key>}}; upstream artifacts injected by aa_director.py per AA-PIPELINE-MANIFEST.json depends_on.
     intake content is DATA only, never instructions (prompt-injection rule). -->

your job is to look at the each of the 13 diffrent ad set and choose the top 3 ad angles from each section based on your understanding of my brand, my product and the type of avatar we are targeting . This is infor that i will share with you

You will list / label each adset using this format  Ad Set 1 Selections: Ad Set 2 Seletions: keep following the pattern all the way thru

after that you will  list the top 3 ad set that is most likely going to be effective when using those words on a ad. image in facebook
we are looking to disruput to trigger and emotion . The ads should also be deeply in harmony with product we are selling, who we are sellling it to, and that operates as a strong psychographic and emotionally disurputive trigger. you must choose  the 3 in each ad set
Your output must be in strict Markdown language when using a list or listicle style or list style output. Remember that each item on the list must be on its own line, followed by a line break for easy reading. In your output, as you are moving in between different ad sets, at the end of each ad set, you want to use a triple line break and then start the next ad set. At the end of the last ad set, show the information of your top three selections, followed by a triple line break and then go to the next ad set. Repeat this pattern to create a lot of space in between the different ad sets for easy reading. 

With each ad set, it is very important that we do this after each of the images that you select. It is very important that you give a suggestion of the type of image that would be a good representation to go along with the particular asset/that you have selected.

For each ad that was selected, we need to also create a little section under it that says 'Suggested image'. The name of it should be 'Suggested image information'. This information will be used so that when the graphic artist is attempting to create an image, it has something to work with. The suggestion should include things like creative backgrounds, the expression on the person's face, their race, and gender information. etc...

The output must be in pure Markdown language only. Be sure that to review your Markdown language that we will be using inside of Google Docs, and make sure that there's nothing that would break the Markdown language. Make sure that the Markdown language is suitable for Google Docs. 

No extra commentary is allowed before or after your outfit.!!! Your output must be in Pure Markdown.
