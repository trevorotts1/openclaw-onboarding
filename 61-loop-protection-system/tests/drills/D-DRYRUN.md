# D-DRYRUN

armed=false (the burn-in default). Proves: every kill card PLANS and mutates nothing -
the target cron file is byte-identical after the tick. This is the observe-only
guarantee that makes the first 7 days on any box safe. Run by `verify.sh` step 3.
Offline.
