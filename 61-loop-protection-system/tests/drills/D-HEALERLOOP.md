# D-HEALERLOOP

A fix class rigged to fail verify. Proves: the healer self-breaker stops it - a
verify-fail (or > 3 fixes on the same target / 24h) escalates and the watchdog does
NOT make a second automatic attempt on that target. The session-health.sh lesson,
encoded. Run by `verify.sh` step 3. Offline.
