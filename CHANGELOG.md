## [v19.23.0]  -  2026-07-10  -  fix(skill-23): honor provenanced dept declines (C1) + true SOP/library gate before closeout stamp (C4)

Merges `fable-fix/onb-skill23` off fresh origin/main (v19.22.0) as the serial onboarding writer. Wave-1 functional fix (judge 9.0). Scoped to `23-ai-workforce-blueprint/scripts` (build-workforce.py, canonical_decline.py, qc-completeness.sh, verify-library-gate.sh + a new adversarial guard test) plus the lockstep version markers `bump-version.sh` rolls (which include 23's own repo-locked skill-version.txt). No client names, no secret values, no Anthropic runtime identifiers in the diff.

- **C1 — provenanced department declines honored at BOTH build-state levels.** `canonical_decline.py` + `build-workforce.py` now apply an operator's provenanced dept decline consistently so a declined vertical is not re-materialized; an UNprovenanced bare "no" still does NOT skip a vertical (the provenance gate holds). Proven by the new `test-decline-provenance-guard.sh` (incl. T4: no closeout/finisher script may write declinedDepartments/intentionalScope).
- **C4 — a TRUE SOP/library gate runs before the closeout stamp.** `verify-library-gate.sh` + `qc-completeness.sh` block `buildCompletedAt` from being stamped over a failed SOP/library gate, so a half-built workforce can't be marked complete.
- **Gates green (raw, this QC).** `test-decline-provenance-guard.sh`, `test-department-floor.sh` (12/0), `test-reconciliation-engine.sh`, `test-sop-substance-models.sh`, `test-gate-scope-exemptions.py` (15/0) all PASS. The two pre-existing, unrelated failures the judge flagged — `test-trio-gate.sh` (missing-DA-operator-note assertion) and `test-gate-company-dir-resolution.sh` T3 — were confirmed to behave IDENTICALLY on clean origin/main (v19.22.0) vs the merged tree (diffed), so they are NOT introduced or worsened by this branch. `py_compile` + `bash -n` clean on all changed files.
- **Also corrects a version-marker drift from the v19.22.0 release (fix-forward).** The presentation release bumped `51-signature-presentation/skill-version.txt` (1.0.5 -> 1.0.6) without rolling Skill 51's SKILL.md top-level frontmatter `version:` (left at 1.0.5), tripping `skill-frontmatter-version-guard`. Both Skill 51 markers are rolled here to `1.0.7` (skill-version.txt must also change or CI guard G3 fires), so the frontmatter guard and G3 are both green again.
- **Version roll** — repo rolled v19.22.0 -> v19.23.0 via `scripts/bump-version.sh` (all 11 markers in lockstep; 23's repo-locked skill-version.txt + SKILL.md frontmatter roll to v19.23.0). Annotated tag `v19.23.0` cut on the release commit.

