# PRESENTATION DEPT — LIVE LEDGER (2026-08-24)

End-to-end diagnostic + fix ledger. Every fix: ID, what, why, file, status, QC verdict.

## Diagnosis — why the department skips steps / stalls / QC is cosmetic

| Symptom | Root cause(s) |
|---|---|
| Skipping steps | F01 bulk route cert bypass; F02 qc_check.py orphaned (48 codes unenforced); F03 verifier exceptions degrade to PASS; F04 precondition ledger trusts attestation rows blindly; F05 sacred structure hashes never checked at runtime |
| Timeouts | F06 no-timeout subprocess fallback when reaper unimportable; F07 unbounded KIE 429 submit loop; F08 batch render resume re-bills all slides; F09 stale-task sweep yanks long renders back to backlog (>24h in_progress); F10 client submit fetch has no timeout |
| Kanban not moving | F11 deployed intake UI hardcodes presentation_type=from_scratch → bridge raises UngroundedDeckTypeError → every app-submitted deck dies before a card exists; F12 bridge marks session processed while card creation soft-failed; F13 one poison session kills whole poll batch; F14 QC auto-scorer cannot close presentations tasks (no cert path) → cards stall in review forever |
| QC not enforced | F15 engine close() mints PROCESS-CERTIFICATE but never blocks on integrity_pass=False; F16 geometry/spelling gates warn-only unless env set; F17 QC cap-exhaustion overridable by self-written token |

## Fix checklist

| ID | Severity | What | Why | File(s) | Status | QC |
|---|---|---|---|---|---|---|
| F01 | BLOCKER | Bulk route: require presentations process_certificate on move→done | Raw UPDATE bypasses transition() cert gate entirely | blackceo-command-center src/app/api/tasks/bulk/route.ts | APPLIED (backup route.ts.bak-presfix-20260824) | QC PASS (Gemini 3.7 Flash) |
| F02 | BLOCKER | Wire qc_check.py into the delivery boundary gate (P9-DELIVER pre-delivery) | 48 declared mechanical AF codes enforced by nothing | run_signature_deck.py | APPLIED (backup run_signature_deck.py.bak-presfix-20260824) — QC-CHECK GATE added before prove-deck, fail-closed | QC PASS (Gemini 3.7 Flash, grouped F02+F06+F17+F18+F19) |
| F03 | MAJOR | Verifier exception = FAIL, not degraded PASS | Crashing measurer equals passing measurer | phase_verifiers.py | APPLIED (backup phase_verifiers.py.bak-presfix-20260824) — new `_degraded_allowed()` mirrors runner's test/CI contract; crash + whole-engine-unavailable sites now fail closed with AF-*-CRASH/MISSING codes in production, NOTE-degrade preserved under PRESENTATION_ALLOW_DEGRADED_VERIFIERS=1 / CI / OPENCLAW_TEST / .test-context; behavioral proof: prod FAIL, all 4 markers PASS | QC PASS (Gemini 3.7 Flash) |
| F04 | MAJOR | Precondition reader: ignore attestation rows without status=='done' + substance_verified | Hand-written row satisfies chain for later phases | build_deck.py check_phase_preconditions + canonical_render_guard.py attested_phase_ids (same hole, delivery boundary) | APPLIED (backups build_deck.py.bak-presfix-20260824, canonical_render_guard.py.bak-presfix-20260824) — rows count only when status=='done' AND substance_verified is True; render special-case preserved; 6-case + 3-case behavioral proofs pass; full-suite run exposed stale test fixture (test_delivery_guard_selfblock wrote bare id-only rows — exactly the forged shape F04 now rejects); fixture rewritten to attest_phase shape (backup tests/test_delivery_guard_selfblock.py.bak-presfix-f04-20260824); full suite 1053 passed / 2 skipped / 0 failed | QC PASS (Gemini 3.7 Flash, grouped F04+F07+F08) |
| F05 | BLOCKER | verify.sh + blend_voice_governance --prove: enforce sacred-structure-hashes.json | Structure drift silently weakens every prover | 51-signature-presentation verify.sh, scripts/blend_voice_governance.py | APPLIED (backups verify.sh.bak-presfix-20260824, scripts/blend_voice_governance.py.bak-presfix-20260824) — new --prove-pin mode fail-closed (missing pin / missing file / any drift → rc=3); verify.sh runs it as check 1a; proofs: happy rc=0 (7 files), tamper/pin-deleted/file-deleted all rc=3 with named diff | QC PASS (Gemini 3.7 Flash) |
| F06 | MAJOR | Loud warning + hard timeout on reaper-missing fallback | Silent infinite-hang path restored | run_signature_deck.py (3 dispatch sites: executor :2990, render :3053, notes-sync :3091) | APPLIED (backup run_signature_deck.py.bak-f06-20260824) — all 3 bare subprocess.run fallbacks now carry the same hard cap as the reaper path + loud WARNING naming the missing module; syntax verified, 3 warning sites + 3 capped calls confirmed | QC PASS (Gemini 3.7 Flash, grouped F02+F06+F17+F18+F19) |
| F07 | MAJOR | Cap the KIE 429 submit retry loop | Unbounded hang at submit stage | build_deck.py (both submit loops: batch Phase A + per-slide path; new KIE_SUBMIT_MAX_429=15, env-overridable) | APPLIED (backup build_deck.py.bak-presfix-20260824) — sustained throttling now raises after N consecutive 429s and the slide FAILS instead of hanging forever; poll side already deadline-bounded by POLL_MAX_SECONDS; bounded-429 behavioral proof passes | QC PASS (Gemini 3.7 Flash, grouped F04+F07+F08) |
| F08 | MAJOR | Batch render resume: reuse existing verified PNGs before resubmit | Crash mid-render re-bills ALL slides | build_deck.py render_slides_batch (Phase A resume-reuse block + Phase B rendered seeding) | APPLIED (backup build_deck.py.bak-presfix-20260824) — ledger entry counts ONLY when completed=True + output_path exists + sha256 matches + PNG magic re-check; reused slides seeded into rendered[] so Phase B polls only new tasks; 4-case proof: good→REUSE, sha-mismatch/file-gone/in-flight→RESUBMIT | QC PASS (Gemini 3.7 Flash, grouped F04+F07+F08) |
| F09 | MAJOR | Stale sweep: exempt presentations in_progress <72h with recent task_events activity | Long renders demoted mid-run to backlog | blackceo-command-center src/lib/jobs/stale-task-sweep.ts | APPLIED (backup stale-task-sweep.ts.bak-presfix-20260824) — exemption requires dept==presentations AND in_progress AND age<72h AND hasRecentTaskActivity (events row within 24h window, own env var PRESENTATIONS_ACTIVITY_WINDOW_HOURS, sqlTime/hoursAgo house pattern, fails closed to no-activity so dead runs age out); Gemini QC: v1 FAIL (no activity check) → v2 FAIL (window==ceiling) → v3 PASS; tsc clean | QC PASS (Gemini 3.7 Flash) |
| F10 | MINOR | Client fetch timeout + retry UI in interview app pages | Hung sink leaves spinner forever | interview-app/pages/index.html | APPLIED (backups pages/index.html.bak-presfix-20260824, deployed-r2/public/index.html.bak-presfix-f10) — new fetchWithTimeout wrapper (20 s AbortController timer + 2 retries with backoff + friendly "took too long" message); writeIntakeFile + triggerDepartment both route through it; download-fallback preserved on sink failure; applied to BOTH repo and deployed-r2 variants; proof: node --check OK both, simulated hanging endpoint → 3 aborts then rejected with retryable message after retries | QC PASS (Gemini 3.7 Flash) |
| F11 | BLOCKER | Redeploy-source parity: repo index.html (asks presentation_type) is canonical; mark deployed-r2 public/index.html superseded + port payload builder | Every live submission crashes the bridge before any card is created | interview-app/deployed-r2/public/index.html | APPLIED (backup index.html.bak-presfix-20260824) — QUESTIONS array now byte-identical to repo (15 questions incl. presentation_type order 0); payload builder never fabricates; JS-parse verified | QC PASS (Gemini 3.7 Flash) |
| F12 | BLOCKER | Bridge poll: dept_start_deferred must NOT be marked processed | Card silently never created, never retried | interview-app/bridge/intake_bridge.py cmd_ingest/cmd_poll | APPLIED (backup intake_bridge.py.bak-presfix-20260824) — now returns rc=5 so poll leaves session unprocessed | QC PASS (Gemini 3.7 Flash, grouped F12+F13) |
| F13 | MAJOR | Per-session try/except in poll loop | One poison session crash-loops every poll | interview-app/bridge/intake_bridge.py cmd_poll | APPLIED (same backup) | QC PASS (Gemini 3.7 Flash, grouped F12+F13) |
| F14 | BLOCKER | QC scorer done-transition: read stored cert from task row (registration already satisfied via PATCH); if none, hold review with explicit event naming missing cert instead of throwing | Cards pass QC ≥8.5 then throw PRECONDITION_PROCESS_CERTIFICATE and stall in review forever | blackceo-command-center src/lib/qc-scorer.ts | APPLIED (backup qc-scorer.ts.bak-presfix-20260824) | QC PASS (Gemini 3.7 Flash) |
| F15 | BLOCKER | Engine close(): integrity_pass=False must block terminal DONE | Cert minted with gaps still reaches DONE | presentation_job/phases.py close() | APPLIED (backup phases.py.bak-presfix-20260824) | QC PASS (Gemini 3.7 Flash) |
| F16 | MAJOR | Default PRESENTATION_SLIDE_GEOMETRY_ENFORCE=1 in canonical entry export | Pixel/spelling QC warn-only by default | presentation-canonical-entry.sh | APPLIED (backups presentation-canonical-entry.sh.bak-presfix-20260824 ×2 — template copy 3 export blocks, scripts copy 1) — `${VAR:-1}` pattern mirrors KIE_PROMPT_GATE contract: canonical run enforces AF-TEXT-OVERFLOW / AF-SPELLING / AF-TYPE-SIZE-MEASURED by default, explicit =0 still opts out; bash -n SYNTAX_OK both copies; behavioral proof: unset→"1", pre-set "0" respected | QC PASS (Gemini 3.7 Flash) |
| F17 | MAJOR | QC cap exhaustion override requires UNSKIPPABLE_QC_PHASES refusal (route through load_skip_approvals contract) | Self-writable token waives terminal QC failure | run_signature_deck.py _run_qc_loop | APPLIED (backup run_signature_deck.py.bak-f17-20260824) — override now consults load_skip_approvals (refuses QC phases per AF-QC-SKIP, validates owner_msg_id via oracle); direct bd._owner_skip_approved consult removed from the loop; ForgedApprovalError caught → no override; behavioral proof: forged AF-COPY-QC record w/ real-looking timestamp+msg_id at cap 4/4 → rc=7 EXIT_QC_EXHAUSTED, attest never fired; AST proof: no bd._owner_skip_approved call in loop body code | QC PASS (Gemini 3.7 Flash, grouped F02+F06+F17+F18+F19) |
| F18 | MAJOR | Atomic attestation writes (temp+replace) in attest_phase/_write_render_record_from_existing | Crash truncates entire attestation chain | run_signature_deck.py | APPLIED (backup run_signature_deck.py.bak-f17-20260824) — new `_atomic_write_json` (tmp+os.replace, tmp cleanup in finally); converted all 3 process_manifest.json write sites: attest_phase :453, _write_render_record_from_existing :401, declare-plan msg_id record :1015; py_compile OK; behavioral proof: attest→valid JSON + no tmp residue, second attest appends without clobber | QC PASS (Gemini 3.7 Flash, grouped F02+F06+F17+F18+F19) |
| F19 | MINOR | Empty timestamp in skip approval rejected | Skips validation branch | run_signature_deck.py load_skip_approvals (timestamp block ~:1282) | APPLIED (backup run_signature_deck.py.bak-f17-20260824) — `if ts:` inverted to reject empty timestamp with named rejection ("a skip with no timestamp is not a verifiable owner decision"); behavioral proof w/ stubbed oracle: empty ts → REJECTED (stderr names branch), valid ts + resolved msg_id → still PASSES (no over-blocking) | QC PASS (Gemini 3.7 Flash, grouped F02+F06+F17+F18+F19) |
| F20 | MINOR | qc_aggregate rounding floor (round down to 4dp, compare raw) | 8.49996 rounds to pass | qc_aggregate.py (~:343 computed_average) | APPLIED (backup qc_aggregate.py.bak-presfix-20260824) — `round()` → `math.floor(raw*1e4)/1e4`; added math import; proof: 8.49996 old=8.5 PASS (wrong) new=8.4999 FAIL (right); exactly-8.5 and genuinely-above still pass | QC PASS (Gemini 3.7 Flash) |
| F21 | MAJOR | Worker /api/intake completeness: reject intakes missing required curated answers | Server accepts anything | interview-app worker/src/index.js + deployed-r2 worker | APPLIED (backups worker/src/index.js.bak-presfix-20260824, deployed-r2/src/index.js.bak-presfix-20260824) — new REQUIRED_BRIEF_FIELDS (10 required+block_gate brief fields from canonical deck-intake-questions.json) + PRESENTATION_TYPE; validateIntakeCompleteness rejects 422 naming each missing field; node --check SYNTAX_OK both; behavioral proof: {} → 11 missing, complete → 0, whitespace-only caught | QC PASS (Gemini 3.7 Flash, grouped F21+F22) |
| F22 | MAJOR (upgraded) | Repo worker router dead-code fix (parts[0] vs parts[1]) | Deploying repo worker verbatim breaks everything | interview-app worker/src/index.js | APPLIED (backup worker/src/index.js.bak-presfix-20260824, same file as F21) — route() conditions reindexed for the leading "api" segment: /api/intake POST, /api/intake/list GET, /api/dept-start POST were all unreachable 404s; routeSessions() had the SAME off-by-one (mint/get/answers/complete all dead); node --check OK; 8-route behavioral proof all PASS | QC PASS (Gemini 3.7 Flash, grouped F21+F22) |
| F23 | MINOR | Golden example refreshed to v51 five-gate plan | Teaches authors a plan that skips two gates | golden-quest declared_plan/process_manifest fixtures | APPLIED (backups declared_plan.json.bak-presfix-20260824, process_manifest.json.bak-presfix-20260824) — plan now 7 steps incl. P-SP-CLAIM 0.14 + P-SP-INTAKE-TRACE 0.16 (roles match v51 manifest exactly); process_manifest: 7 attestations (both missing gates carry real captured PASS outputs), signature_presentation_gates 3→5; role drift fixed (P0A-INTAKE→director-of-presentations, P-SP-STRUCTURE→signature-presentation-architect); proof: consistency assertions pass + all five gate commands re-run live rc=0 | QC PASS (Gemini 3.7 Flash) |
| F24 | MINOR | prove_sp_routing Signal 4: scan request/brief text as documented | Gate narrower than advertised | 51-signature-presentation/scripts/prove_sp_routing.py | APPLIED (backup prove_sp_routing.py.bak-presfix-20260824) — Signal 4 now scans entries/sp_entries free text (answer/normalized/value/note, case-insensitive markers incl. "signature presentation"/"signature_presentation"); presentation_type check also honors `normalized`; proof: free-text request now FIRES AF-SP-TYPE-UNDECLARED, no-phrase ledger still PASSES, normalized variant caught, golden + self-test both PASS | QC PASS (Gemini 3.7 Flash) |
| F25 | MINOR | intake_trace_check load_bank_questions fail-closed on missing spec | Mispathed spec guts conversation gate silently | intake_trace_check.py | APPLIED (backup intake_trace_check.py.bak-presfix-20260824) — new MissingQuestionBank raised when either bank is missing/corrupt/parses-to-zero; CLI catches → exit 3 with named JSON error; engine call sites (build_deck :9203, slice1_gate_verifiers :998) already except-Exception→AF-INTAKE-BATCH FAIL so raising turns silent no-op into named gate failure; proof: self-test ALL PASS incl. new Test 0, mispathed spec rc=3, golden transcript still rc=0, engine path raises | QC PASS (Gemini 3.7 Flash) |
| F26 | MAJOR | Parent children count: exclude 'review' from done-count | Parent shows progress counting unverified children | blackceo-command-center presentations/children route | APPLIED (backup route.ts.bak-presfix-20260824) | QC PASS (Gemini 3.7 Flash) |
| F27 | MINOR | fix_bundle_complete run_bundle_gate: enforce min_bytes from DELIVERABLE_AUDIT_SPEC | 1-byte stub passes FIX-8 gate | fix_bundle_complete.py | APPLIED (backups fix_bundle_complete.py.bak-presfix-20260824, tests/test_fix8_bundle_complete.py edited in place, tests/test_webinar_builder.py.bak-presfix-20260824) — check_bundle_complete now requires each deliverable ≥ its spec min_bytes floor (deck_pptx 1 MB, audio_mp3 512 KB, etc.); selftest + pytest fixtures rewritten to write at-floor files + new undersized-stub case; proof: 80/80 bundle-suite tests pass, selftest PASS, standalone 8-byte audio stub → AF-BUNDLE-INCOMPLETE rc=1 naming audio_mp3 only | QC PASS (Gemini 3.7 Flash; v1 FAIL was diff-artifact — stat() guarded by lexists+try in live file; rebuilt diff PASS) |
