# U001 — Manifest home and resolver: DRY manifest resolution

**status:** fixed
**builder-model:** deepseek-v4-pro
**fixer-model:** deepseek-v4-pro
**fixer-loop:** 1
**scored-at:** 2026-07-28

## Fix branch
`unit/U001-manifest-home-and-resolver`

## Fix head commit
`dd276d45efaf8ab1ba786a551df65016e36428ce`

## Merge commit
`e243e8ffa7e9094230852acb35ae4c4dce41f93f` ("Land U001: Manifest home and resolver — merge + ticket + digest")

**Correction 2026-07-30 (ticket-hash audit, [Claude Sonnet 5 ×1]):** the head
commit recorded above was originally written as
`55bc81a10a36ef01f68178427b19724e18794730` — that commit exists (branch
`fix-u001-wf`, same author/topic/timestamp-minute) but is **not an ancestor
of `origin/main`**. The commit actually merged is `dd276d45...`
("fix(U001): wire manifest_source resolvers and complete unit deliverables"),
which **is** `e243e8ff`'s first parent and **is** an ancestor of
`origin/main`. Verified via `git show --stat dd276d45` (touches
`sync_check.py`, `run_signature_deck.py`, `gate_integrity_check.py`,
`runner_gate_integrity_check.py` as claimed below, plus `install.sh` /
`update-skills.sh` manifest-placement additions) and
`git merge-base --is-ancestor dd276d45 origin/main` → true. The stale
`55bc81a...` was never touched by this correction and remains unmerged on
its orphan branch.

## Builder
`[deepseek-v4-pro x1] fix-unit repair (U001)`

## What was fixed
Replaced local `_find_repo_root` and `_resolve_manifest` function definitions in 4 scripts with imports from the single canonical `manifest_source.py` module.

### Files changed
- `sync_check.py` — removed `_find_repo_root`, `_first_existing`; uses `resolve_manifest(HERE)` / `resolve_ruleset(HERE)`
- `run_signature_deck.py` — removed `_find_repo_root`; `load_manifest()` now delegates to `resolve_manifest(HERE)`
- `gate_integrity_check.py` — removed `_find_repo_root`, `_resolve_manifest`; `load_manifest()` uses `resolve_manifest(HERE)[0]`
- `runner_gate_integrity_check.py` — same as gate_integrity_check.py

### Verification
- All 4 files: no local `_find_repo_root` or `_resolve_manifest` functions
- All 4 files: `manifest_source` import present
- All 4 files compile as valid Python
- Downstream variable names preserved (`MANIFEST`, `MASTER_RULESET`, `_REPO_ROOT`, `RENDER_DECK`)
