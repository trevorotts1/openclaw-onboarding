# U006 — Stop the entry script guessing which scripts folder to use

unit: U006 (Unit 6)      surface: code      slice: SPEC/units/U006.md
branch: unit/U006-stop-guessing-scripts  pushed: yes        head commit: 49dae078
tree: ~/Downloads/July 25th Presentation Spec Documents/repos/.judge-U006-44684aee, detached HEAD 49dae078, content hash of touched directory verified
builder: DeepSeek V4 Pro — Stop the entry script guessing which scripts folder to use (U006) — 1 agent
judge:   [Kimi 2.6 ×1] Quality-control Stop the entry script guessing which scripts folder to use (U006) — 1 judge
card-shape: PASS (13 of 13 mandatory sections present)
scores: |1:8|2:8|3:9|4:9|5:10|6:9|7:9|8:9|9:9|10:9|  → 8.9
        checksum: 8+8+9+9+10+9+9+9+9+9 = 89; 89/10 = 8.9
evidence: see below — every acceptance criterion run with quoted commands and outputs
break-it: see below — all adversarial cases tried, including empty/malformed input and mutant proofs
mutation-proof: see below — two independent mutations, both turned red, reverted green
fail-closed: none tripped
verdict: PASS                    timestamp: 2026-07-28T22:55:00Z

---

## Acceptance Criteria Run (with quoted commands and outputs)

### VERIFY — builder commands

1. `bash -n 23-ai-workforce-blueprint/scripts/presentation-canonical-entry.sh && echo "entry parses"`
   Output: `entry parses`

2. `bash -n update-skills.sh && bash -n install.sh && echo "installers parse"`
   Output: `installers parse`

3. `bash tests/unit/presentation-deps-gate.test.sh; echo "deps-gate test exit=$?"`
   Output: `presentation-deps-gate: PASS=59 FAIL=4` / `deps-gate test exit=1`

   Note: The 4 FAILs are pre-existing on `origin/main` (bypass-scan, scan, owner_skip_approval, clean-run-dir — same 4 on `main` at `b4b84917`). The U006 assertions (section E) all PASS:
   - `entry: refuses to autodetect the scripts dir` — PASS
   - `entry: names the department default` — PASS
   - `entry: refuses the skills-template copy` — PASS
   - `entry: prints the scripts-dir provenance` — PASS
   - `entry: seven-candidate list is gone` — PASS
   - `entry: a stated-but-wrong --scripts-dir EXITS 2 (executed, not read)` — PASS
   - `entry: the refusal is on stderr at runtime` — PASS
   - `entry: a valid --scripts-dir is NOT refused (rc=0)` — PASS

4. Stated-but-wrong `--scripts-dir`:
   ```
   bash 23-ai-workforce-blueprint/scripts/presentation-canonical-entry.sh \
     --run-dir "$R" --scripts-dir /nonexistent-abc --plan > "$L/wrong.log" 2>&1
   echo "stated-wrong exit=$?"
   ```
   Output: `stated-wrong exit=2`
   Stderr tail: `FATAL [presentation-canonical-entry.sh]: --scripts-dir / $SCRIPTS_DIR was set but does not hold both build_deck.py and run_signature_deck.py. Refusing to autodetect — a wrong scripts directory silently invalidates GATE 1b, GATE 3 and every phase attestation. Point it at the materialized department: --scripts-dir ~/.openclaw/workspace/departments/Presentations/scripts`

5. Stated skills-template directory:
   ```
   bash presentation-canonical-entry.sh --run-dir "$R" \
     --scripts-dir "$PWD/23-ai-workforce-blueprint/templates/role-library/presentations/scripts" \
     --plan > "$T/template.log" 2>&1
   echo "template-stated exit=$?"
   ```
   Output: `template-stated exit=2`
   Stderr tail: `FATAL [presentation-canonical-entry.sh]: refusing to run against the skills-TEMPLATE copy (...) That tree carries its own manifest and is not the governed department. Use --scripts-dir ...`

---

### QC-1 — skills layout, no flag → must refuse (exit 2)

```
T=$(mktemp -d)
mkdir -p "$T/23-ai-workforce-blueprint/scripts"
rsync -a <Tree A>/23-ai-workforce-blueprint/templates "$T/23-ai-workforce-blueprint/"
cp <Tree A>/23-ai-workforce-blueprint/scripts/presentation-canonical-entry.sh "$T/23-ai-workforce-blueprint/scripts/"
cp <Tree A>/23-ai-workforce-blueprint/scripts/deck-build-guard.sh "$T/23-ai-workforce-blueprint/scripts/"
R=$(mktemp -d); mkdir -p "$R/working/checkpoints"
OPENCLAW_WORKSPACE="$T/no-such-workspace" \
  bash "$T/23-ai-workforce-blueprint/scripts/presentation-canonical-entry.sh" \
  --run-dir "$R" --plan > "$T/qc1.log" 2>"$T/qc1.err"
echo "exit=$?"
```
Output: `exit=2`
Stderr tail: `canonical scripts (build_deck.py + run_signature_deck.py) not found at the materialized department (...) Refusing to autodetect. Either materialize the Presentations department, or state the directory explicitly: --scripts-dir DIR`
Result: PASS — the template tree is sitting right there, two directories away, and the script refused it.

---

### QC-2 — GATE 0 armed/absence loud

Run A (guard missing):
```
OPENCLAW_WORKSPACE="$W" bash "$DS/presentation-canonical-entry.sh" --run-dir "$R" --plan \
    >"$W/a.out" 2>"$W/a.err"
echo "runA_rc=$?"
python3 - "$W/a.out" "$W/a.err" <<'PY'
import sys
out = open(sys.argv[1]).read(); err = open(sys.argv[2]).read()
print("DISABLED on stderr:", "DISABLED" in err)
print("DISABLED on stdout:", "DISABLED" in out)
sys.exit(0 if ("DISABLED" in err and "DISABLED" not in out) else 1)
PY
echo "runA_check=$?"
```
Output: `runA_rc=8` / `DISABLED on stderr: True` / `DISABLED on stdout: False` / `runA_check=0` → PASS

Run B (guard present):
```
OPENCLAW_WORKSPACE="$W" bash "$DS/presentation-canonical-entry.sh" --run-dir "$R" --plan \
    >"$W/b.out" 2>"$W/b.err"
echo "runB_rc=$?"
python3 - "$W/b.out" <<'PY'
import sys
out = open(sys.argv[1]).read()
banner = "GATE 0 — FRONT-DOOR SELF-SCREEN" in out
passed = "GATE 0 PASSED" in out
print("GATE 0 banner on stdout:", banner)
print("GATE 0 PASSED on stdout:", passed)
sys.exit(0 if (banner and passed) else 1)
PY
echo "runB_check=$?"
```
Output: `runB_rc=8` / `GATE 0 banner on stdout: True` / `GATE 0 PASSED on stdout: True` / `runB_check=0` → PASS

Mutant proof (send warning back to stdout):
```
python3 - "$DS/presentation-canonical-entry.sh" <<'PY'
import pathlib, sys
p = pathlib.Path(sys.argv[1]); before = p.read_text()
hits = [l for l in before.split("\n") if "DISABLED" in l and ">&2" in l]
print("lines carrying DISABLED and >&2 before mutation:", len(hits))
assert hits, "nothing to mutate"
after = "\n".join(l.replace(">&2", "") if ("DISABLED" in l and ">&2" in l) else l for l in before.split("\n"))
assert after != before, "the mutation changed nothing"
p.write_text(after); print("mutated: warning now goes to stdout")
PY
OPENCLAW_WORKSPACE="$W" bash "$DS/presentation-canonical-entry.sh" --run-dir "$R" --plan \
    >"$W/m.out" 2>"$W/m.err"
python3 - "$W/m.out" "$W/m.err" <<'PY'
import sys
out = open(sys.argv[1]).read(); err = open(sys.argv[2]).read()
print("DISABLED on stderr:", "DISABLED" in err)
print("DISABLED on stdout:", "DISABLED" in out)
sys.exit(0 if ("DISABLED" in err and "DISABLED" not in out) else 1)
PY
echo "runA_mutant_rc=$?"
```
Output: `lines carrying DISABLED and >&2 before mutation: 1` / `mutated: warning now goes to stdout` / `runA_mutant_rc=1` with `DISABLED on stderr: False` / `DISABLED on stdout: True` → PASS (mutant turned red, comparator works)

---

### QC-3 — installer copies both files

```
python3 - update-skills.sh "$W2/fn.sh" <<'PY'
import pathlib, sys
src = pathlib.Path(sys.argv[1]).read_text().splitlines()
start = next(i for i, l in enumerate(src) if l.startswith("colocate_presentation_entry() {"))
end   = next(i for i in range(start + 1, len(src)) if src[i] == "}")
pathlib.Path(sys.argv[2]).write_text(chr(10).join(src[start:end + 1]) + chr(10))
print(f"extracted update-skills.sh:{start + 1}-{end + 1}")
PY
run_colocate "$W2" "$PWD"
```
Output: `extracted update-skills.sh:1934-1957` / `PRESENT+EXEC presentation-canonical-entry.sh` / `PRESENT+EXEC deck-build-guard.sh` → PASS

Negative control (deck-build-guard.sh removed from source):
```
rm -f "$S2/23-ai-workforce-blueprint/scripts/deck-build-guard.sh"
run_colocate "$W3" "$S2"
```
Output: `MISSING deck-build-guard.sh` → PASS

Also extracted `colocate_presentation_entry()` from `install.sh:2550-2579` — present, named function with `obs_resolve_workspace` per that file's own convention. Verified.

---

### QC-4 — no literal home path

```
git fetch origin main
git diff origin/main > "$Q4/u006.diff"
python3 - "$Q4/u006.diff" <<'PY'
import sys
added = [l for l in open(sys.argv[1]) if l.startswith('+') and not l.startswith('+++')]
bad   = [l for l in added if '/Users/' in l]
print(f"{len(added)} added lines checked, {len(bad)} contain a literal home path")
for l in bad: print("  ", l.rstrip())
sys.exit(1 if bad else 0)
PY
echo "qc4_rc=$?"
```
Output: `1645 added lines checked, 0 contain a literal home path` / `qc4_rc=0` → PASS

Mutant:
```
printf '+DEPT_SCRIPTS_DEFAULT="/Users/someone/.openclaw/workspace/departments/Presentations/scripts"\n' >> "$Q4/mutant.diff"
python3 - "$Q4/mutant.diff" <<'PY'
...same...
PY
echo "qc4_mutant_rc=$?"
```
Output: `1646 added lines checked, 1 contain a literal home path` / `qc4_mutant_rc=1` → PASS

---

## Break-it pass

| Case | Method | Outcome |
|------|--------|---------|
| Empty input | `--scripts-dir ""` with env `SCRIPTS_DIR=""` | exit 8 (materialized department missing, falls through to default path check, expected behaviour) |
| Stated dir missing both files | `--scripts-dir /nonexistent-abc` | exit 2, `Refusing to autodetect` on stderr |
| Stated dir has only one file | `--scripts-dir "$T/scripts"` with only `build_deck.py` | exit 2, `Refusing to autodetect` on stderr |
| Skills layout, no flag | Run from skills tree with no `--scripts-dir` | exit 2, refused template tree |
| Skills-template explicitly stated | `--scripts-dir <template-path>` | exit 2, refused by name |
| Co-located without guard | Guard missing at co-located path | stderr warning with DISABLED, non-fatal |
| Valid --scripts-dir | `--scripts-dir <real-dept>` | exit 0, accepted |
| Mutant: refusal message changed | Text assertion test | FAIL=6 (red) |
| Mutant: return 2 -> return 0 | Script runs through to later gate | FAIL=6 (red) |

No hidden stubs, no faked values, no leaked secrets, no unverified claims.

---

## Mutation proof

**Mutation 1:** Changed `Refusing to autodetect` → `SILENTLY autodetecting` in the `die()` refusal message.
Red: `FAIL: entry: nothing on stderr said 'Refusing to autodetect' (rc=2)` (FAIL=6 total).
Reverted: GREEN (FAIL=4, same pre-existing failures).

**Mutation 2:** Changed `return 2` (stated-but-wrong) → `return 0` in `resolve_scripts_dir()`.
Red: `FAIL: entry: stated-but-wrong --scripts-dir exited 8, expected 2` (FAIL=6 total).
Reverted: GREEN (FAIL=4, same pre-existing failures).

Both mutations prove the suite can turn red. No hollow tests.

---

## Counts re-derived

- `assert_entry_has`: 19 (card claimed 15 → 19; measured 19)
- `assert_entry_lacks`: 1 (card claimed 0 → 1; measured 1)
- `FAKERUNNER` terminator at line 358; executing test at line 394; `after fixture: True`
- Added lines against `origin/main`: 1645
- Literal home paths in added lines: 0
- `9000` character prompt floor: unchanged in all touched files

---

## Targets confirmed

- `resolve_scripts_dir()` exists in `presentation-canonical-entry.sh:139`
- `SCRIPTS_DIR_STATED` exists, set at `:97` and `:106`
- `SCRIPTS_DIR_RC` exists, handled at `:150-162`
- `case` block for skills-template exists at `:171-177`
- `colocate_presentation_entry()` exists in `update-skills.sh:1934` and `install.sh:2550`
- `deck-build-guard.sh` is referenced in GATE 0 at `:288`
- `assert_entry_lacks()` exists in test file
- `assert_entry_has` count moved 15 → 19

---

## Corrections filed

None. The card matches the implementation.

---

## Fail-closed rules

| Rule | Status | Notes |
|------|--------|-------|
| D.1  | PASS | No empty functions, TODOs, or faked output |
| D.2  | PASS | Tests run, mutation proof turned red twice |
| D.3  | PASS | No secret leaks, no home paths, no identifiers |
| D.4  | PASS | Every claim independently reproduced with quoted output |
| D.5  | PASS | No AI authorship trailers in commit messages |
| D.6  | PASS | No standing integrity alarm on the repo |
| D.7  | PASS | No scaffolding in deliverables |
| D.8  | PASS | All counts re-derived with judge's own commands |
| D.9  | PASS | Every symbol confirmed to exist |
| D.10 | PASS | No `find -type f` census in any changed file or check |
| D.11 | PASS | None of the six project-specific blocks apply |
| D.12 | PASS | 9000-character floor unchanged; no working check weakened |

---

## Merge-time truth gates

Not applicable — the merge-writer handles these.

---

*Ticket written by judge [Kimi 2.6 ×1] at 2026-07-28T22:55:00Z. Prior state was FAIL (gate 1.00) due to stub/branch mismatch, fixed by git-recovery pass. Fresh round-1 judge.*

---

merge commit: e9b734329762ca1af9338d57343cedd0ba0579b9
ancestor of main: proven 2026-07-29T14:49:33Z
batch tag: v21.4.10

**Correction 2026-07-29T15:05Z:** the merge-commit hash recorded above was
originally written as `abd29891cd231b9e931ca41002250ddb57087da8` — that is
actually the "Ripple v21.4.9: U013" commit, not U006's own merge commit. All
four tickets in the v21.4.10 batch (U006, U027, U049, U070) had their
merge-commit hashes shifted by one position against `git log`'s true order.
Corrected here to U006's actual merge commit (`e9b73432...`, "Land U006: Stop
guessing scripts — merge + ticket") by [Claude Sonnet 5 ×1], re-deriving from
`git log origin/main --oneline` directly rather than trusting the prior
ticket text.
