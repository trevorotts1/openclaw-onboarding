# U019 — Add the teleprompter to the list of files the client is handed
merge commit: (pending — filled by the backfill commit)
ancestor of main: pending push
batch tag: (pending ripple)

unit: U019 (Unit 19)   surface: decision   repository: openclaw-onboarding
branch: unit/U019-teleprompter-client   head commit: 67f088103539400ed1b7722dd92615c812f4c0dc
verdict: PASS
scores: |1:9|2:9|3:9|4:9|5:8|6:9|7:9|8:9|9:9|10:9|  → 8.9   (checksum 89/10; gate 8.5)
judge: [Claude Opus 5 (1M context) x1] Review + merge U019 — 1 reviewer/merge-writer

D02 ratified 2026-07-26 (decider: operator). The teleprompter becomes the SIXTH
client-package file. Stage 1 of the warn-mode rollout only — `teleprompter_html` is in
`CLIENT_PACKAGE_WARN_ONLY`, so a missing teleprompter warns and still passes; stage 3
(hard enforcement) is deliberately a later unit.

manifest_version 29 -> 30; MIN_MANIFEST_VERSION moved with it to 30 (the card's
`Touches:` said "25 -> 26", a stale literal its own step 8 forbids using: the value on
disk at branch-cut was 29, not 25). MANIFEST-SOURCE.txt created NEW with
content_sha256=3ffcbaf4744edc981a188d2430cc57894c05c5a5aab7c36e578e22bbe893e99d; no prior
stamp existed and none was invented.

Mutation proof reproduced by the reviewer: baseline=0, mutant=1 with four real assertion
failures (not a collection error), reverted=0, restore verified by sha256, tracked
delivery_gate.py unmodified throughout.

HANDOFFS: U049 and U051 must each re-cut MANIFEST-SOURCE.txt's content_sha256= in their
own commit (this unit makes the stamp binding; a stale stamp is exit 7 on every engine
invocation). Verified at merge time: MANIFEST-SOURCE.txt did NOT exist on origin/main, and
neither U049's nor U051's branch was an ancestor of main — so both obligations are live.
U004 owns the two box manifest copies and must land them before or with the fleet roll,
never after the code.

Full review evidence: <project>/QUALITY-CONTROL/tickets/U019.md
