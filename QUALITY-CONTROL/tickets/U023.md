# U023 -- Operator Box Migration Roll-Forward (110-111)

**status:** fixed
**builder-model:** sonnet
**fixer-model:** ds/deepseek-v4-pro
**fixer-loop:** 1
**scored-at:** 2026-07-28
**fix-applied:** all

## Fix branch
`unit/U023-fail-closed-render-guards`

## Fix head commit
`1c7b93f1` (on origin/main, includes a6989a5c)

## Original verdict

**Score: FAIL**

### What went wrong
The original `unit/U023-fail-closed-render-guards` branch remote tip (`0269655f`) contained a commit with message "U050: fix palette-check live bugs" -- wrong unit's work. The U023 build deliverable (`docs/U023-operator-box-migration-roll-forward.md`, commit `142d004e`) was never merged to origin/main, and the fix commit `a6989a5c` was already present on origin/main without any U023 branch tracking it.

### What the fixer did
1. Verified `a6989a5c` (U023: repoint render-guard _FIX2_SYMBOLS at real functions, fail-closed, de-duplicate pre-delivery) is ancestor of origin/main
2. Rebased `unit/U023-fail-closed-render-guards` onto origin/main `1c7b93f1`
3. Restored deliverable `docs/U023-operator-box-migration-roll-forward.md` from build commit `142d004e`
4. Created this ticket

### Evidence from fix commit a6989a5c (already on origin/main)
- **File 1:** `23-ai-workforce-blueprint/templates/role-library/presentations/scripts/canonical_render_guard.py` (+35/-7)
  - Repointed `_FIX2_SYMBOLS` from three non-existent names to two real exports: `check_canonical_render_path` and `check_image_qc_vision`
  - Deleted `_chk_local_canvas` entry entirely (no such function ever existed; AF-LOCAL-CANVAS is emitted from inside the two checks)
  - `run_fix2_checks` fail-closed: `getattr` returning non-callable now raises RuntimeError instead of silently `continue`-ing
  - `ImportError` degrades gracefully; all other exceptions re-raise as RuntimeError
  - `guard_pre_delivery` adds `(pre-delivery re-check)` tag to de-duplicate defense-in-depth findings
- **File 2:** `23-ai-workforce-blueprint/templates/role-library/presentations/scripts/tests/__init__.py` (new)
- **File 3:** `23-ai-workforce-blueprint/templates/role-library/presentations/scripts/tests/test_fix2_symbols.py` (+83 new)
  - test_a: all names callable on build_deck
  - test_b: exactly 2 entries, no `_chk_` prefix
  - test_c: bogus name raises RuntimeError
  - test_d: owner_skip_approval suppresses its AF code; malformed tokens don't

### Mutation proof
- test_c verifies fail-closed: a bogus entry raises RuntimeError with the AF code in the message
- test_b verifies no stale `_chk_` prefix names can re-enter
