# U024 — Blended-persona governance

**status:** judged
**builder-model:** fable (DeepSeek V4 Pro)
**judge-model:** ollama-cloud/kimi-k2.6
**fixer-model:** deepseek-v4-pro
**fixer-loop:** 2 (prior), now fresh round-1 after clean-slate void
**scored-at:** 2026-07-28
**head-commit:** d47323f1

## Fix branch
`unit/U024-persona-governance`

## Judge verdict

**Score: FAIL** (7.70 / 10)

### Findings

1. **Integration gap — the governance module is NOT actually called by the presentation pipeline engine.** Commit `52fe9eaf` claims "wire persona governance call," but `presentation_job/phases.py` (the engine that runs pipeline phases) and `presentation_job/__main__.py` (the CLI entrypoint) contain ZERO imports of or calls to `presentation_job.persona.resolve_for_phase()`, `governance_banner()`, or `structure_warn_check()`. The `BLEND_PHASE_FOR` mapped phases (`P-SP-STRUCTURE`, `P4-COPY`, `P-SP-P3-HYGIENE`, `P4-PROMPT`) have `executor_kind: None` in `PIPELINE-MANIFEST.json` and no `executor_cmd` referencing the persona module. No `import presentation_job.persona` or `from presentation_job import persona` appears anywhere in production Python outside `test_persona_governance.py`. The module docstring states "called once per pipeline phase" but this claim is unfulfilled.

   Evidence:
   ```
   $ grep -rn "resolve_for_phase\|governance_banner\|structure_warn_check" \
     --include="*.py" . | grep -v "def " | grep -v test_persona_governance | grep -v persona.py:
   (no output)
   ```
   The only references to these functions in the entire repository are:
   - their definitions in `persona.py`
   - their invocations in `test_persona_governance.py`
   - unrelated `_load_blend_module()` calls in `06-ghl-install-pages`, `23-ai-workforce-blueprint/scripts/persona-selector-v2.py`, and `49-signature-funnel`

   The `run_signature_deck.py` diff (commit `52fe9eaf`) only removes `manifest_source` imports and replaces them with an inline `_find_repo_root()` — no persona governance wiring. The `build_deck.py` diff removes `COPY_HOOK_EXEMPTION_ENFORCED` U067 code and adjusts comment wording — again, no persona governance wiring.

   This directly contradicts the unit's title "Install and actually call the blended-persona governance." Installation exists; the call does not.

2. **Branch touches non-U024 files without clear U024 rationale.** The 58-file diff includes changes to `build_deck.py` (removing U067 hook-exemption code, 34 lines deleted), `run_signature_deck.py` (replacing manifest_source imports, 22 lines changed), and `tests/test_producers.py` / `tests/test_resume.py` (both deleted, 760 lines total). The deleted tests are not U024-related and their removal is not justified in commit messages. Per spec-common "branch shall contain ONLY unit-related changes."

3. **Persona.py lacks a `__main__` guard / CLI for direct invocation.** While `governance_banner()` and `structure_warn_check()` can be called, there's no `if __name__ == "__main__"` block, making them inaccessible to shell-level health checks or install verification scripts.

### Passing evidence (what IS correct)

- **Tests (10/10):** `tests/test_persona_governance.py` passes all 10 tests in 0.03s.
- **Mutation proof:** Mutating `BLEND_TIMEOUT_S` from 30 to 999 causes `test_timeout_wall_fires` to fail with `AssertionError: BLEND_TIMEOUT_S must be 30, got 999`. Reverted to 30, all 10 tests pass.
- **No Co-Authored-By trailers:** `git log --format="%b" origin/main..HEAD | grep -c "Co-Authored"` returns 0 across all 7 commits.
- **Template edit (step 4):** `create_role_workspaces.py` governing-personas.md template corrected in commit `52fe9eaf` to say the blend is "GOVERNING, not advisory" with explicit references to `blend_voice_governance.py`, `persona_for_job.py`, and `SKILL51_BLEND_GOVERNS=0` flag.
- **Materialization assertions:** Post-materialize sha256 assertion for `blend_voice_governance.py` in `create_role_workspaces.py` — a diverged copy raises `RuntimeError`.
- **Sacred structure hashes:** Pin file has 7 entries; `structure_warn_check()` returns `checked=7, mismatched=[], pin_file_found=True`.
- **U98 and U114 conformance:** Both `tests/unit/u98-blend-governs-product-voice-engines.test.py` and `tests/unit/u114-no-exemption-blend-governance-conformance.test.py` pass independently.
- **Fail-closed design:** Timeout wall (`BLEND_TIMEOUT_S=30`), flag-off path (`SKILL51_BLEND_GOVERNS=0`), unreachable module BLOCKS, graceful state.json absence handling, `executor.shutdown(wait=False)` for true timeout semantics.

### Mutation proof

- **Finding 1 (timeout):** `BLEND_TIMEOUT_S = 30 -> 999` in `persona.py` -> `test_timeout_wall_fires` FAILED with `AssertionError: BLEND_TIMEOUT_S must be 30, got 999`. Reverted to 30 -> all 10 tests PASSED.
- **Finding 2 (structure):** `structure_warn_check()` returns `checked: 7, mismatched: []` against the real pin file; tampered pin (one hash flipped to `"0" * 64`) correctly reports the flipped key in `mismatched` and never calls `governed_deck_voice`.
- **Finding 3 (AI trailers):** No `Co-Authored-By` trailers remain on any U024 commit. `git log --format="%b" $(git log --oneline origin/main..HEAD --format="%H") | grep -c "Co-Authored"` returns 0.

### Ten-category rubric

| Category | Score | Evidence |
|---|---|---|
| 1. Code correctness (structure, imports, types) | 7.5 | Well-structured stdlib-only module with type hints. Deduction: module not wired into engine, so imports are dead code in production. |
| 2. Test coverage and depth | 7.0 | 10 tests cover all functions. Mutation proof works. Deduction: no integration test verifying the engine calls the module. |
| 3. Documentation clarity | 9.0 | Extensive docstrings explaining "why" (timeout rationale, BLEND_PHASE_FOR mapping, flag-off path). |
| 4. Fail-closed / defensive design | 8.5 | Timeout wall, flag-off path, unreachable module BLOCKS, graceful state.json handling, `executor.shutdown(wait=False)`. |
| 5. Mutation proof | 9.0 | Hard-guard on `BLEND_TIMEOUT_S`, captured `timeout=` kwarg assertion, structure tamper detection. |
| 6. No AI trailers / proper authorship | 10.0 | 0 Co-Authored-By trailers across 7 commits. |
| 7. Separation of concerns | 9.0 | Clean split: `persona.py` is the caller, `blend_voice_governance.py` is the governance seam, `sacred-structure-hashes.json` is the pin. |
| 8. Integration with existing codebase | 3.0 | **Critical gap.** The module exists and is tested but NOT called by `phases.py`, `__main__.py`, `run_signature_deck.py`, or `build_deck.py`. The unit title is "Install and actually call" — the call is missing. |
| 9. Template / materialization edit | 8.0 | Governing-personas template corrected and materializer has sha256 assertion. Deduction: the assertion is in `create_role_workspaces.py`, not in a standalone post-install verification script. |
| 10. Branch cleanliness (only unit-related changes) | 6.0 | 58 files touched; `build_deck.py` and `run_signature_deck.py` changes are structural cleanup, not governance wiring. `test_producers.py` and `test_resume.py` deleted without U024 justification. |

**Average: 7.70**

### Break-it pass

- **Break 1:** Delete `blend_voice_governance.py` from the dept scripts dir. `governance_banner()` returns `"ON"` because it falls through to repo/worktree layout (sibling `51-signature-presentation`). The banner should say `UNREACHABLE` when the dept copy is missing — but this is a deployment-path issue, not a logic bug. The banner IS truthful about which path it resolved.
- **Break 2:** Set `SKILL51_BLEND_GOVERNS=0`. `resolve_for_phase()` correctly returns `{"persona_governance": "legacy-intake-tone"}` — the flag-off path works.
- **Break 3:** Remove `sacred-structure-hashes.json`. `structure_warn_check()` returns `{"checked": 0, "mismatched": [], "pin_file_found": False}` — graceful degradation, no crash.

### Fail-closed rules check

- Rule 2 (timeout mutation proof): PASS. `BLEND_TIMEOUT_S` hard-guard at line 125 of test; mutation to 999 turns red.
- Rule 3.5 (structure warn never blocks): PASS. `structure_warn_check()` returns dict, never raises, never exits non-zero.
- Rule 8 (Co-Authored-By purge): PASS. 0 trailers.
- Rule 9 (branch only unit changes): FAIL. Non-U024 files modified/deleted without justification.
- Rule 10 (module actually called): FAIL. The governance module is not invoked by the production engine.

### Recommendation

To pass, the builder must:
1. **Wire `resolve_for_phase()` into the engine's phase loop** — import `presentation_job.persona` in `phases.py` or `__main__.py` and call `resolve_for_phase(run_dir, phase_id)` for each of the four mapped phases before dispatch. Alternatively, add the call in `run_signature_deck.py` or `build_deck.py` at the appropriate pre-render/copy/prompt/structure entrypoints.
2. **Justify or revert non-U024 file changes** — the `build_deck.py` U067 cleanup and `run_signature_deck.py` manifest resolution changes should be in a separate unit or justified as U024 prerequisites. The deleted `test_producers.py` and `test_resume.py` should be restored unless their deletion is independently justified.
3. **Add an integration test** that verifies the engine (or at least one production entrypoint) imports and calls `resolve_for_phase()` or `governance_banner()` during a realistic pipeline phase execution.

---
**[Kimi 2.6 ×1] Quality-control Install and actually call the blended-persona governance (U024) — 1 judge**
