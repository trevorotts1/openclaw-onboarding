# U045 — Remove the mute landmine from the served Command Center's environment file

**status:** blocked-human (Step 0 done, Step 2 NOT done — false evidence corrected 2026-07-30)
**spec card:** `SPEC/units/U045.md`

---

## CORRECTION NOTICE (2026-07-30)

This ticket previously read:

```
merge commit: 8a14c48f8ac4a9e8b73d71a78ebc60dc3e1bc386
ancestor of main: proven 2026-07-29T13:11:52Z
batch tag: v21.4.8
```

**That merge commit citation is FALSE.** Proof:

```
$ git show --stat 8a14c48f
commit 8a14c48f8ac4a9e8b73d71a78ebc60dc3e1bc386
Merge: f20fc0ed fe495980
Author: Trevor Otts <trevor@blackceo.com>
Date:   Wed Jul 29 09:09:57 2026 -0400

    Land U023: merge + ticket

 .../scripts/tests/test_fix2_symbols.py             | 14 +++-
 QUALITY-CONTROL/tickets/U023.md                    | 49 ++++++++++++
 docs/U023-operator-box-migration-roll-forward.md   | 86 ++++++++++++++++++++++
 3 files changed, 145 insertions(+), 4 deletions(-)
```

`8a14c48f`'s real subject is **"Land U023: merge + ticket"** and it touches U023's own files (`canonical_render_guard.py`'s test, `docs/U023-...md`, `QUALITY-CONTROL/tickets/U023.md`). Nothing in it belongs to U045. Citing it here as U045's completion proof means this ticket previously claimed another unit's merge as its own. **Kept here as recorded history of the defect** — `8a14c48f` is U023's commit, not U045's, full stop.

### Corrected citation: `6b0d898f`

Searched all refs (`git log --all --grep`, `git log --all -S`, branch names) for genuine U045 "mute landmine" work, to locate the real Land commit:

- `git log --all --grep='U045' -i` and `-S'u045'` surface two **unrelated** prior units that also carry the `U045` id (this repo has reused the id across spec generations): a 2026-07-23 podcast feature ("transcript-vs-audio sync verification", commit `294f766f`, merged `5bbd918d`) and its own later ripple reference (`0a1d8301`). Neither is this unit.
- The only commit touching *this* unit's actual ticket is **`6b0d898f` "Land U045: merge + ticket"** (2026-07-29 09:09:57 -0400, same run as the batch). Its diff is exactly 4 lines, all of them adding the (then-false) footer above to `QUALITY-CONTROL/tickets/U045.md`.
- Branch `origin/unit/U045-mute-landmine` exists but points at `5e181ceb` ("Stop the updater from emptying a shared AGENTS.md when it cannot read it") — an unrelated commit, 233 files different from `main`. It is a stale/orphaned branch name, not real U045 work.

**`6b0d898f` is the correct Land commit for U045, and its bookkeeping-only diff is the expected shape, not evidence of fabrication.** Per `SPEC/units/U045.md` itself: **"Same commit as: none. This unit produces no repository commit at all"** — the card's own design has no code artifact to merge; this is an ops-only `.env.local` edit on a live box, so its Land commit necessarily carries no code, only this ticket's footer. A Land commit whose entire diff is 4 lines of ticket bookkeeping is therefore exactly what this unit's design predicts, not proof that no legitimate merge commit exists. This was independently confirmed by PR #747's ticket-hash audit (merged, QC'd PASS): `6b0d898f`'s subject is literally "Land U045: merge + ticket," and `git merge-base --is-ancestor 6b0d898f origin/main` confirms it is an ancestor of `origin/main`. Re-verified independently during this reconcile with the same two checks, same result.

**Corrected footer** (supersedes the false `8a14c48f` citation above):

```
merge commit: 6b0d898f74cf5832e4cb564616b785be60750b89
ancestor of main: proven 2026-07-29T13:11:52Z
batch tag: v21.4.8
```

**Correction 2026-07-30 (ticket-hash audit, [Claude Sonnet 5 ×1]):** the
merge-commit hash recorded above was originally written as
`8a14c48f8ac4a9e8b73d71a78ebc60dc3e1bc386` — that is actually **U023's**
merge commit ("Land U023: merge + ticket"), not U045's own. Same
shifted-by-one-position defect as the rest of the v21.4.8 batch. Corrected
to `6b0d898f...` ("Land U045: merge + ticket", 2026-07-29T09:09:57-04:00),
confirmed an ancestor of `origin/main`.

### Wider pattern found — NOT fixed here, flagged for the operator

While tracing the false citation, the same v21.4.8 batch (`U011, U015, U023, U045, U055, U056, U057`) shows every other ticket's footer citing the **previous unit's** own Land-commit instead of its own (an apparent off-by-one in whatever stamped these 7 tickets):

| Ticket | Cites | That hash's real commit |
|---|---|---|
| U011.md | `630690fd` | v21.4.7's ripple/tag commit (previous *batch*) |
| U015.md | `34eea3ab` | "Land **U011**: merge + ticket" |
| U023.md | `f20fc0ed` | "Land **U015**: merge + ticket" |
| U045.md | `8a14c48f` | "Land **U023**: merge + ticket" |
| U055.md | `6b0d898f` | "Land **U045**: merge + ticket" |
| U056.md | `89592dd4` | "Land **U055**: merge + ticket" |
| U057.md | `b1c04658` | "Land **U056**: merge + ticket" |

The true "Land U057" commit, `6459ea4d`, is cited by none of the seven. **This is out of scope for this ticket** (this fix touches only `U045.md`) but is the same false-evidence mechanism, six times over, and should be triaged separately.

---

## STEP 0 — Named Stop paperwork: GENUINELY COMPLETE

Verified independently at the project-level control file (this file lives outside the git repo, at `<project-root>/CONTROL/QUESTIONS-FOR-HUMAN.md`, per the card's own absolute path):

```
$ grep -a -n -i "U045" "CONTROL/QUESTIONS-FOR-HUMAN.md"
392:# Question 5 — U045 — remove the mute landmine from the served Command Center's environment file
```

The full question (context, three options a/b/c, recommendation (a), cost, and risk-if-never-done) is present starting at line 392, matching the card's Step 0 text verbatim. **This part of the unit is done and has real evidence.**

---

## STEP 2 — the live `.env.local` edit: NOT DONE

Per the card's own procedure (`SPEC/units/U045.md`, "Step 2 — APPLY"), a completed apply requires, on the served Command Center box:

- `0` live assignments of `OWNER_NOTIFY_TELEGRAM_DISABLED` in `.env.local`
- `1` commented history line naming `U045` and the original value
- a `.env.local.bak-u045-mute-landmine-<STAMP>` backup, made with `cp -p`, taken **before** the edit
- `ecosystem.cc-prod.config.cjs` sha256 unchanged (never touched — Step 3)

**As reported to this ticket, the live box does not match that state:** `OWNER_NOTIFY_TELEGRAM_DISABLED` is still a **live, uncommented** assignment (value `'0'`), not a commented history line — meaning the flag was overwritten in place rather than commented out per the card's procedure. **No `.env.local.bak-u045-mute-landmine-*` file exists** — the one-and-only restore point Step 2 requires was never created. An unrelated "loopfix" dated 2026-07-28 appears to have changed the value in place instead of following this unit's apply procedure. This ticket does not independently re-read the live file (out of scope — see below); this status is recorded as given and should be re-confirmed with the card's own name-only, read-only pre-flight (Step 1) before any further action.

**Do not treat the live box's current `'0'` value as evidence this unit was applied.** The card's own procedure requires the flag be *commented out*, not merely set to `'0'` — a live `'0'` is not the target end state and leaves the duplicate-switch condition (`CHANGE TO MAKE`, Step 3) unresolved.

---

## REMAINING — REQUIRES OPERATOR AUTHORIZATION

None of the following may be run by an unattended builder or by this fix. All are Named Stop 9.9 (live production Command Center):

1. **Re-run Step 1 pre-flight** (`SPEC/units/U045.md`, read-only, name-only) to get a current, accurate reading of `.env.local` line/value, `ecosystem.cc-prod.config.cjs:105`, and the two-process loader probe — **before** relying on any previously reported state, including the one recorded above.
2. **If pre-flight confirms the landmine is still live and uncommented**, and the operator approves option (a) from Question 5 (delete/comment the line):
   - Take the backup first: `cp -p .env.local ".env.local.bak-u045-mute-landmine-<STAMP>"` (exactly one new backup; eight already exist as of the card's writing — do not add a ninth by hand).
   - Run the card's Step 2 apply script (rewrites the live assignment to a commented history line; asserts exactly one live assignment exists before touching anything).
   - Do **not** touch `ecosystem.cc-prod.config.cjs` (Step 3 — the pin stays).
   - Decide separately whether a restart is covered by the approval (Step 4) — the change is inert either way, so there is no urgency to restart.
   - Run Step 5's post-check and record its output in this ticket.
3. **QC-1/QC-2/QC-3/QC-4** from the card must be run by a different agent than the builder, per the card's own QC section, before this ticket can be marked anything other than `blocked-human`.
4. Update this ticket's `DONE WHEN` live-portion checklist (card lines under "Live portion — requires explicit human approval") with real command output once the above is authorized and run. Do not mark this ticket `fixed`/`PASS`/merged-anything until a genuine commit or live-artifact proof exists — per the card, the live artifacts (backup file + commented line + unchanged ecosystem hash) **are** the proof for this unit; there is no code commit to point to instead.

---

## Batch/date metadata (accurate, kept for record)

- Step 0's paperwork commit corresponds to the same run recorded in `CONTROL/CHANGELOG.md` under `## v21.4.8 — 2026-07-29T13:11:52Z`, which legitimately lists `U045: Mute landmine` alongside U011/U015/U023/U055/U056/U057. The batch tag `v21.4.8` and timestamp `2026-07-29T13:11:52Z` are real and independently confirmed (`git show 7aa04398`, `CHANGELOG.md:524`). **Only the specific "merge commit" hash was false; the batch/date were not.**
