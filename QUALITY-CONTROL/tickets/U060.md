# U060 — Anthology writer role-specific personality/safety

**status:** fixed
**builder-model:** fable
**judge-model:** fable
**fixer-model:** fable
**fixer-loop:** 1
**scored-at:** 2026-07-23

## Fix branch
`unit/U060-anthology-writer-safety-fix`

## Fix head commit
`37b1f25b` — **UNRESOLVED (2026-07-30 ticket-hash audit, [Claude Sonnet 5 ×1])**

**This hash and every commit this ticket narrates (`1dbf75d0`, `bfd6b7a3`,
and the implied "Land U060" event) are REJECTED as belonging to the OTHER
July 23–24 project that reused unit names U001–U073 (per
`CONTROL/RESUME-PLAYBOOK.md`'s 55 leftover branches), not this one:**
- `37b1f25be309dc62132ad73a020d35d1244f57f9` is dated
  **2026-07-23T23:01:39-04:00**, authored by `trevor@ottssolutions.com`.
- Its only reachable "Land U060" commit, `cb41a380` ("Land U060: Anthology
  writer role-specific personality/safety — merge + ticket + digest"), is
  also dated **2026-07-23** and is authored `trevor@ottssolutions.com` — and
  is **not an ancestor of `origin/main`** (`git merge-base --is-ancestor
  cb41a380 origin/main` → false). `git log origin/main | grep -a -i u060`
  returns nothing: no commit reachable from `origin/main` mentions U060 at
  all.
- The cherry-pick source this ticket cites, `1dbf75d0` ("fix(U060): exclude
  user-template files from identity gate fill-in checks"), is also dated
  2026-07-23 and is also not an ancestor of `origin/main`.
- The rebase target this ticket cites, `bfd6b7a3` ("Land U074: Credential
  value-emitting checks to presence-only..."), is dated 2026-07-23 and
  authored `trevor@ottssolutions.com` — itself a different project's unit.

**The underlying functionality (`_is_user_template()` in
`scripts/qc-assert-agent-identities-resolved.sh`, and
`tests/unit/assert-agent-identities-resolved.test.sh`) does exist on
`origin/main` today**, but arrived via `5e181cebb5468313d1074940e0cc64f9ea32e24e`
("Stop the updater from emptying a shared AGENTS.md when it cannot read
it", 2026-07-27T07:53:23-04:00, correctly authored `trevor@blackceo.com`)
— a large, unrelated bulk commit (hundreds of files: `.gitattributes`,
`.githooks/`, `.github/workflows/*`, etc.) whose subject has nothing to do
with U060 and which was not judged or merge-written as a U060 unit. No
dedicated, evidence-backed "this project's U060 merge" commit could be
found. Per audit instructions, no hash is invented here — this ticket is
marked **UNRESOLVED** pending a real, independently-judged merge event for
U060 in this project, or a decision that `5e181ceb` should be retroactively
credited (operator call, not this auditor's).

## Judge verdict

**Score: FAIL**

### What went wrong
The original `unit/U060-anthology-writer-safety` branch was stacked on a stale merge-base (51 commits behind origin/main), producing 84 stale-file artifacts in the diff. The branch only changed a single file (`scripts/qc-assert-agent-identities-resolved.sh +23/-4`) but the stale base made the diff look huge.

Additionally, no behavioral tests existed for the `_is_user_template` exclusion logic — the only tests were inside the script's `--self-test` mode, which didn't cover the U060-specific exclusion patterns.

### What the fixer did
1. Rebased onto `origin/main` (HEAD: `bfd6b7a3`). Cherry-picked only the U060-relevant commit `1dbf75d0` (`fix(U060): exclude user-template files from identity gate fill-in checks`). Diff is now clean: 1 file, +23/-4.
2. Added behavioral test script `tests/unit/assert-agent-identities-resolved.test.sh` with 4 tests:
   - **A:** Root IDENTITY.md with template markers is skipped (no false-positive)
   - **B:** 18-proactive-agent/upstream-original/assets/SOUL.md with template markers is skipped (no false-positive)
   - **C:** Non-excluded agent file with unresolved fill-in prompt IS detected (exit non-zero)
   - **D:** Non-excluded agent file with `{{GENERATED_FOR}}` IS detected (exit non-zero)
3. Mutation proof: inverted `_is_user_template` return values → all 4 tests failed (RED). Reverted → all green.

### Fixer notes
- The `IDENTITY\.md$` exclusion pattern matches ALL IDENTITY.md files in any path, not just root. This is intentional — the regex anchors to the filename, not the path. Test D uses SOUL.md (not IDENTITY.md) to avoid this exclusion and prove that template markers in non-excluded files ARE still caught.
- The existing `--self-test` mode's 5 tests remain functional alongside the new behavioral suite.
