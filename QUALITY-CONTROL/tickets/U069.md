
## Merge attempt 2026-07-29T15:05Z — blocked-gate-red, NOT merged

**status:** blocked-gate-red (declined by merge-writer [Claude Sonnet 5 ×1] after a fresh, independent gate-suite run — the ticket's own PASS 9.5 verdict does not hold against the current state of `main`)

Branch `unit/U069-shell-injection-fix` (6b653b5e) merges cleanly with no
textual conflicts (confirmed with a non-destructive `git merge-tree`
dry run, and the previously-recorded add/add conflict with U013 is gone now
that U013 is on `main`). The merge was staged, committed locally, and then
**backed out before push** because the gate suite went RED:

```
cd 23-ai-workforce-blueprint/templates/role-library/presentations/scripts
python3 -m pytest tests/test_presentation_job.py -q
```

`tests/test_presentation_job.py::TestU069ShellInjectionFix::test_u069_shell_injection_blocked`
**FAILED**: `AssertionError: SECURITY FAILURE: sentinel .../PWNED_U069 exists!`
— the injected `echo hello; touch <sentinel>` command actually ran the
`touch`, meaning shell metacharacters WERE interpreted. 17 of 18 tests in the
file passed; this one did not. (1 failed, 17 passed.)

**Root cause:** U069's fix (tokenise-first `shlex.split`, `shell=False`) only
covers the two primary executor call sites the judge's AST audit counted —
`presentation_job/phases.py:206` and `presentation_job/report.py:158`
(both confirmed `shell=False` in the current tree). It does **not** cover
`presentation_job/heal.py`, which was added by a different unit (U016,
merged 2026-07-28T22:15:56Z — before U069's most recent PASS judgment at
2026-07-29T10:24:52Z) and contains its own, separate re-execution path for
the same manifest-controlled `phase.executor_cmd` string:

- `heal.py` `rung2_regenerate()` (line ~39): `subprocess.run(cmd, shell=True, cwd=str(engine.run_dir), timeout=budget, capture_output=False)` where `cmd = (phase.executor_cmd or "").replace("{run_dir}", str(engine.run_dir))` — raw string substitution, `shell=True`, no tokenisation.
- `heal.py` `rung3_alt_route()` (line ~63): identical pattern against `alt_cmd` (`executor.alt_cmd`).

The failing test's own trace shows the P0A-INTAKE phase's artifact check
failing and the engine's **regeneration/heal path** (`rung2_regenerate`)
re-running the exact same injected `cmd` string via `heal.py`'s `shell=True`
call — which is why the sentinel file gets created even though
`phases.py`'s primary path is fixed correctly.

**This means the U069 ticket's own QC-4 claim — "every subprocess call in
every module of the package checked by AST: exactly 2 calls, both
shell=False" — is FALSE against the current tree.** The AST audit that
produced that claim did not traverse `heal.py`, or ran before `heal.py`'s
two `shell=True` calls existed in the judge's own working copy. Either way,
independently re-running the suite right now, on `main` plus this branch,
reproduces a real, exploitable shell-injection path through the healing
retry logic, not a flaky or environmental failure.

**Disposition:** NOT merged. `main` is unchanged — the locally-committed
merge was reset with `git reset --hard origin/main` before any push, so
nothing was pushed red. This ticket needs a fixer pass that extends U069's
tokenise-first/`shell=False` fix to `heal.py`'s `rung2_regenerate` and
`rung3_alt_route`, then a fresh independent judge round (Law 7: different
model than whoever writes the `heal.py` fix), before the merge-writer can
land it.

merge commit: NOT MERGED
ancestor of main: NOT MERGED — blocked-gate-red
batch tag: none (not part of any batch)
