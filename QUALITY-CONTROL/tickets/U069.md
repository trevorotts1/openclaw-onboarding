**CURRENT STATUS (2026-07-30): FIXED AND MERGED.** The `blocked-gate-red` /
`NOT merged` disposition below was correct **when written** (2026-07-29T15:05Z)
but is now stale — the fix landed on `main` through three later commits, none
of which re-used this ticket's original branch. See the dated correction entry
at the bottom of this file for full evidence. The section immediately below is
kept verbatim as an accurate record of why the first attempt was correctly
declined; it is not being rewritten.

## Merge attempt 2026-07-29T15:05Z — blocked-gate-red, NOT merged

**status:** blocked-gate-red (declined by merge-writer [Claude Sonnet 5 ×1] after a fresh, independent gate-suite run — the ticket's own PASS 9.5 verdict does not hold against the current state of `main`)

Branch `unit/U069-shell-injection-fix` (6b653b5e) merges cleanly with no
textual conflicts (confirmed with a non-destructive `git merge-tree`
dry run, and the previously-recorded add/add conflict with U013 is gone now
that U013 is on `main`). The merge was staged, committed locally, and then
**backed out before push** because the gate suite went RED:

```
cd 23-ai-workforce-blueprint/templates/role-library/presentations/scripts
python3 -m pytest tests/test_presentation_job.py -q
```

`tests/test_presentation_job.py::TestU069ShellInjectionFix::test_u069_shell_injection_blocked`
**FAILED**: `AssertionError: SECURITY FAILURE: sentinel .../PWNED_U069 exists!`
— the injected `echo hello; touch <sentinel>` command actually ran the
`touch`, meaning shell metacharacters WERE interpreted. 17 of 18 tests in the
file passed; this one did not. (1 failed, 17 passed.)

**Root cause:** U069's fix (tokenise-first `shlex.split`, `shell=False`) only
covers the two primary executor call sites the judge's AST audit counted —
`presentation_job/phases.py:206` and `presentation_job/report.py:158`
(both confirmed `shell=False` in the current tree). It does **not** cover
`presentation_job/heal.py`, which was added by a different unit (U016,
merged 2026-07-28T22:15:56Z — before U069's most recent PASS judgment at
2026-07-29T10:24:52Z) and contains its own, separate re-execution path for
the same manifest-controlled `phase.executor_cmd` string:

- `heal.py` `rung2_regenerate()` (line ~39): `subprocess.run(cmd, shell=True, cwd=str(engine.run_dir), timeout=budget, capture_output=False)` where `cmd = (phase.executor_cmd or "").replace("{run_dir}", str(engine.run_dir))` — raw string substitution, `shell=True`, no tokenisation.
- `heal.py` `rung3_alt_route()` (line ~63): identical pattern against `alt_cmd` (`executor.alt_cmd`).

The failing test's own trace shows the P0A-INTAKE phase's artifact check
failing and the engine's **regeneration/heal path** (`rung2_regenerate`)
re-running the exact same injected `cmd` string via `heal.py`'s `shell=True`
call — which is why the sentinel file gets created even though
`phases.py`'s primary path is fixed correctly.

**This means the U069 ticket's own QC-4 claim — "every subprocess call in
every module of the package checked by AST: exactly 2 calls, both
shell=False" — is FALSE against the current tree.** The AST audit that
produced that claim did not traverse `heal.py`, or ran before `heal.py`'s
two `shell=True` calls existed in the judge's own working copy. Either way,
independently re-running the suite right now, on `main` plus this branch,
reproduces a real, exploitable shell-injection path through the healing
retry logic, not a flaky or environmental failure.

**Disposition:** NOT merged. `main` is unchanged — the locally-committed
merge was reset with `git reset --hard origin/main` before any push, so
nothing was pushed red. This ticket needs a fixer pass that extends U069's
tokenise-first/`shell=False` fix to `heal.py`'s `rung2_regenerate` and
`rung3_alt_route`, then a fresh independent judge round (Law 7: different
model than whoever writes the `heal.py` fix), before the merge-writer can
land it.

merge commit: NOT MERGED
ancestor of main: NOT MERGED — blocked-gate-red
batch tag: none (not part of any batch)

---

## CORRECTION 2026-07-30 — fix landed via three separate commits; the
## `blocked-gate-red` / NOT-merged disposition above is now FALSE (stale)

This ticket asserted `status: blocked-gate-red` / `Disposition: NOT merged`
for over a day after the underlying gap was actually closed. **That was a
false negative**: the work landed on `main`, but through three follow-on
commits outside this ticket's original branch, and nobody updated this file
when it happened. Recorded here so the ticket matches reality; the decline
record above is left untouched because it was correct at the time and
explains *why* the first merge attempt was right to fail.

### What actually closed the gap, independently re-verified today

All three commits below were re-verified directly against `origin/main`
(`e5361ab5ef1472a48f6e6e0e4276af8dce1855fe` at verification time) in a
detached worktree, not taken on faith:

| Commit | `git merge-base --is-ancestor <c> origin/main` | What it did |
|---|---|---|
| `89b0e745d1961e1a4abb58a19d2874e6f8e2a489` (cherry-pick of `6b653b5e`, landed via **PR #734**) | **IS ANCESTOR** | `phases.py._run_script_phase` and `report.py`'s dispatch path: `shlex.split` + `shell=False` at both original call sites. |
| `d8e25519354129288c6ad03480a4370b93c9a80c` (**PR #734**) | **IS ANCESTOR** | Closes the exact gap that blocked the original merge: `heal.py`'s `rung2_regenerate()` and `rung3_alt_route()` no longer re-derive `cmd`/`alt_cmd` with `.replace()` + `shell=True`. Both now call a single shared `Engine._build_executor_argv()` (defined in `phases.py`, used by `_run_script_phase`, `rung2_regenerate`, and `rung3_alt_route` alike), so there is exactly one tokenise-then-substitute path in the package instead of two independently-drifting ones. |
| `977dcc407e7c5d832e36149feb9e9078dbd65abd` (**PR #740**) | **IS ANCESTOR** | Closes a *third*, previously unknown site found by a full-package grep rather than a file-scoped one: the module-level `report.dispatch()` function (as opposed to `Reporter._dispatch()`, which #734 had already fixed) was still `shell=True`, and `watchdog.py` calls that module-level function directly. Also closed `__main__.py::cmd_sweep_undeliverable`, which had its own hand-rolled `subprocess.run(cmd, shell=True, ...)` over the same env var instead of routing through the shared `dispatch()`. |

PR cross-check via `gh pr view`: **#734** ("U069: shell-injection fix + close
heal.py retry-rung bypass") merged 2026-07-30T10:44:16Z, merge commit
`5392b6b0b0d0be3d1e4c8be655afc6455d8efb19` — confirmed ancestor of
`origin/main`, and both `89b0e745` and `d8e25519` confirmed ancestors of that
merge commit. **#740** ("U069: close report.py's module-level dispatch()
shell-injection bypass") merged 2026-07-30T13:17:45Z, merge commit
`7c2ce02d2afe0a00e25bdc9ef3044c4b42b03571` — confirmed ancestor of
`origin/main`, and `977dcc40` confirmed an ancestor of that merge commit.

### Package-wide grep, re-run today, clean

```
$ grep -a -rn "shell=True" 23-ai-workforce-blueprint/templates/role-library/presentations/scripts/presentation_job/
heal.py:23:    # re-derive the command here. A hand-rolled `.replace()` + shell=True in
heal.py:55:    # local .replace() + shell=True re-derivation.
__main__.py:166:            # do not re-derive a subprocess.run(cmd, shell=True, ...) call
```

All three hits are comments/docstrings warning against reintroducing the
pattern — **zero executable `shell=True` occurrences anywhere in the
package.** Spot-checked the specific sites named in this ticket's decline:

- `phases.py`: `import shlex` (line 4); `_build_executor_argv` tokenises via
  `shlex.split(raw)` (line 199); `_run_script_phase` calls
  `subprocess.run(argv, shell=False, ...)` (line 231).
- `report.py`: module-level `dispatch()` (line 30) does
  `argv = shlex.split(cmd)` (line 47) then `subprocess.run(argv,
  shell=False, ...)` (line 54); `Reporter._dispatch()` (line 160) shares the
  same logic per its own comment.
- `heal.py`: `rung2_regenerate()` (line 21) and `rung3_alt_route()` (line 53)
  both call `engine._build_executor_argv(...)` (lines 28, 64) then
  `subprocess.run(argv, shell=False, ...)` (lines 44, 72) — the exact gap
  named in the 2026-07-29T15:05Z decline is closed.
- `__main__.py`: `cmd_sweep_undeliverable` (line 148) imports `dispatch`
  from `.report` (line 16) and calls it directly (line 170) instead of
  re-deriving its own subprocess call.
- `watchdog.py`: imports `dispatch` from `.report` (line 132) and calls it
  directly (line 133) — the same shared function `__main__.py` uses, so the
  fix in `report.py` covers both callers.

### Process failure — why this ticket was wrong for over a day

The original branch (`unit/U069-shell-injection-fix`, `6b653b5e`) was
correctly declined and reset without pushing, per the 2026-07-29T15:05Z entry
above. The actual fix then landed a different way: `6b653b5e` was
cherry-picked into a new commit (`89b0e745`) alongside a fresh commit closing
the `heal.py` gap (`d8e25519`), both shipped together in PR #734; a second,
independent QC pass later found a third site and closed it in PR #740. Both
PRs merged to `main` on 2026-07-30. **Nobody updated this ticket file when
either PR merged**, so it continued to assert `blocked-gate-red` /
`NOT merged` for hours after the disposition it described was no longer true.
That is the mirror image of a false-completion defect: a ticket claiming
work is *not* done when it *is* done, which risks someone trusting the stale
ticket over the code and attempting to "re-fix" (or, worse, revert) an
already-closed security hole. This entry exists to close that window.

**Corrected disposition:** MERGED. Fix is complete and verified on
`origin/main` as of this entry. The three commits above, landed via PR #734
and PR #740, close every gap identified in the 2026-07-29T15:05Z decline plus
one additional site (`report.py`'s module-level `dispatch()` /
`__main__.py::cmd_sweep_undeliverable`) found by a full-package grep rather
than a file-scoped one.
