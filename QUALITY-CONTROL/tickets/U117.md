# U117 -- Fleet rollout: wave-list fix -- all boxes

- **unit**: U117
- **score**: FAIL (original commit modified specialist identity files, not wave lists)
- **status**: fixed
- **pushed**: yes
- **branch**: unit/U117-wave-list-fix
- **head commit**: 6e13f6b8
- **fixer**: Claude

## Fixer notes (2026-07-23)

### Root cause
The original U117 commit on `unit/U117-archived-skills-wave-fix` modified specialist
identity files (`42-personal-assistant-library/specialists/`), not the wave lists in
`lib-onboarding-state.sh`. The wave-list fix (remove archived skill names) was already
landed on main via U101. U117's deliverable should be the FLEET ROLLOUT SCRIPT that
pushes the fix to all boxes.

### Fix applied
Replaced the incorrect commit with the fleet rollout deliverable:

1. **scripts/roll-wave-list-fix.sh** -- Fleet rollout script that:
   - Reads a roster file (pipe-separated: slug|type|address|onboarding_root)
   - Checks wave-list integrity on each box via qc-assert-wave-list-integrity.py
   - If PASS: reports already-clean. If FAIL: applies the fix.
   - Fix: atomic copy of operator's corrected lib-onboarding-state.sh with backup
   - Supports local/mac/vps box types, dry-run mode, --only filtering, --yes arming pin
   - Writes per-box PASS/FAIL/SKIP results to a JSONL ledger

2. **tests/unit/u117-wave-list-roll.test.sh** -- 22 tests covering:
   - Roster parsing, dry-run detection of clean/dirty boxes
   - Fix application, idempotency, --only filtering
   - Arming-pin enforcement, empty roster rejection
   - JSONL ledger validation
   - 2 mutation-proof pairs (M1: disabled fix copy, M2: removed arming pin) each RED/GREEN

### Mutation proof

**Mutation 1** -- Disable the fix copy (replace `cp "${LIB_SOURCE}" "${tmpfile}"` with early return):
  - RED: phantom "11-superdesign" persists after run (fix never applied)
  - Revert -> GREEN: original script removes phantom, integrity check passes

**Mutation 2** -- Remove arming pin guard (delete the die that enforces --yes):
  - RED: script writes to box without --yes permission
  - Revert -> GREEN: original script refuses without --yes (T7)

### Verification
- `bash -n scripts/roll-wave-list-fix.sh` -- passes
- `bash -n tests/unit/u117-wave-list-roll.test.sh` -- passes
- `bash tests/unit/u117-wave-list-roll.test.sh` -- 22 passed, 0 failed
