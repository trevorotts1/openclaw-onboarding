# U122 — Fleet rollout: install QC gate promotion — all boxes

**status:** fixed
**fixer-model:** fable
**fixer-loop:** 1
**scored-at:** 2026-07-23

## Fix branch
`unit/U122-fleet-install-qc-gate-fix`

## Fix head commit
`c7a8b6f7`

## Judge verdict

**Score: FAIL**

### What went wrong
The original `unit/U122-fleet-install-qc-gate` branch was stacked on U124 commit `7bfd09d3`, producing a diff that included 183 stale-file artifacts. The branch needed a clean re-cut onto `origin/main`.

Additionally, `qc_parse_skill_verdict` in `lib-qc-shared.sh` would return 0 with empty values when the `Result:` line had no proper numeric counts (e.g. "all passed"), causing the QC agent JSON report to carry garbage values for `skill_pass`/`skill_fail`/`skill_warn`.

### What the fixer did
1. Re-cut branch cleanly onto `origin/main` (HEAD: `31f194db`) using `git apply` of only the 3 U122 files (`lib-qc-shared.sh`, `scripts/qc-agent.sh`, `scripts/tests/test-qc-agent-warn-visibility.sh`). Diff: 3 files, +118/-10.
2. Fixed `qc_parse_skill_verdict` with a dual-layer guard:
   - **Awk guard**: `if(p=="" && f=="" && w=="") exit 1` — rejects Result lines where no numeric count is matched
   - **Bash guard**: integer validation on at least one extracted value (`case "$v" in ''|*[!0-9]*)`) — defense-in-depth against non-numeric garbage
3. `bash -n` passed on all 3 files.
4. Test suite `scripts/tests/test-qc-agent-warn-visibility.sh` — 6/6 green.
5. **Mutation proof**: Removed both guards → tested "all passed" Result line → returned 0 with empty `pass=`/`fail=`/`warn=` (RED). Restored both guards → returned 1 (GREEN).
6. Committed as Trevor Otts, no AI trailers. Force-pushed cleaned branch.

### Fixer notes
- The awk guard `$(i-1)+0==$(i-1)` correctly distinguishes numeric fields from non-numeric strings (e.g., `0=="all"` is false in awk), so "all passed" is already rejected at the awk level. The bash guard adds defense-in-depth for any edge case where awk produces whitespace-only output.
- The test mock skill produces "1 passed | 0 failed | 3 warnings" and exits 0, which exercises the STAGE-1 behavior: warnings are visible in JSON but never fail the agent.
