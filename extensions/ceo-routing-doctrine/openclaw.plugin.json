{
  "id": "ceo-routing-doctrine",
  "name": "CEO Routing Doctrine (pre-injection)",
  "description": "Replaces the removed CEO gate with a prompt-injection layer. Injects the routing doctrine (route work, don't self-execute) into every agent turn via before_prompt_build prependSystemContext. No tool-deny \u2014 no loop possible. Honor an explicit human override when the owner asks the agent to do the work personally.",
  "version": "1.0.0",
  "contracts": {
    "tools": []
  },
  "setup": {
    "providers": []
  },
  "main": "./dist/index.js",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "enabled": {
        "type": "boolean"
      },
      "routingPreamble": {
        "type": "string"
      }
    }
  }
}