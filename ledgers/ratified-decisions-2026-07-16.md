# Trevor's RATIFIED DECISIONS — 2026-07-16

**Purpose:** this file is the permanent, durable record of decisions Trevor made on 2026-07-16 about the Skill 6 (blended-persona-kanban v2, 117-unit) build. It exists so **no agent ever re-litigates, re-asks, or re-opens any item below.** Where a decision here conflicts with older text in the master spec (`Downloads/skill6-blended-persona-kanban-MASTER-SPEC-v2-2026-07-13.md`) or the ledger's older evidence prose, **this file wins** — the master spec is NOT edited in place (it is a 552KB historical planning document; edits belong here and in the live ledger rows instead, per standing digest-first doctrine).

Glossary for anyone new to this build: **"U`<n>`"** = Unit `<n>` of the 117-unit Skill 6 decomposition (e.g. "U65" = Unit 65). **"D`<n>`"** = Decision `<n>` in the master spec's decision register (spec section E.3 / the "Other RATIFIED / BINDING decisions" list). **ONB** = the `trevorotts1/openclaw-onboarding` repo. **CC** = the `trevorotts1/blackceo-command-center` repo.

**Note on this file's history across branches.** This exact filename exists independently, with different content, on at least three other still-unmerged branches at the time this copy was created: `chore/ratified-decisions-2026-07-16` (the base pass, U65/U64/D20), `chore/ratified-decisions-2026-07-16-d12-d4` (built on the base pass, adds D12/D4/D15/D19 and a fuller running history), and `fix/gk25-u87-golden-quest-real-run` (PR #599, one entry: GK-25/U87's prompt-floor descope). This copy was created independently on `chore/d23-hold-list-marketing-not-funnels` to record one ruling (the D23 correction below) without importing or depending on any of those branches' unmerged content, for the same reason the GK-25 copy gives for doing the same thing: importing another branch's un-reconciled history risks tripping this repo's own CI guards (e.g. the docs-language guard) on content this PR is not responsible for. Reconciling every branch's history into one committed file on `main` is the serial merge-writer's job at actual merge time; this copy makes no attempt to pre-empt that reconciliation. The header/purpose/glossary paragraphs above are copied verbatim from the sibling branches' copies of this same file, unmodified, to keep the format consistent — everything from the "D23 (correction)" heading down is new content written for this pass.

## Consolidation — 2026-07-17

**What happened.** By the time this pass started, this filename had been created independently, from scratch, on **five** different starting points, because `ledgers/ratified-decisions-2026-07-16.md` does not exist on `origin/main` and each unit that needed to record a ratification created it fresh on its own branch:

| Branch | Tip (at time of this pass) | Ancestor of `main`? | What it added |
|---|---|---|---|
| `chore/ratified-decisions-2026-07-16` | `f7d978425b5b92ffb60d85fd6b4f515c9bfbd29d` | No | D-U65, D-U74, D-U64, D20, D-U22, D-U26, D-U63, FLEET-ROLL/P4 |
| `chore/ratified-decisions-2026-07-16-d12-d4` | `41d2d1f9ff95e81500cac0d53a2e8ecd0f20ea8b` | No | the above (as its own direct ancestor) + D12/D-HL-3, D4/D-A4, D15/D-J1, GK-D4/D19 |
| `chore/ratified-decisions-2026-07-16-d8` | `90c278af36fbdd8fcb9e10cda20a18676de9fb5d` | No | D8/D-C2 only |
| `chore/d23-hold-list-marketing-not-funnels` (PR #611, this branch) | `c25ecfb4ab70d915ccf2c0cfffbe73a3c3033fc5` at the time of this pass | No | D23 (correction) only |
| `fix/gk25-u87-golden-quest-real-run` (PR #599) | `07f8aa90cda7638276e86f033c83e8632b0638a9` | No | GK-25/U87 only |

Ancestry between these five was checked pairwise via `git merge-base --is-ancestor`: only `chore/ratified-decisions-2026-07-16` is an ancestor of `chore/ratified-decisions-2026-07-16-d12-d4` (the latter is the former plus six more commits). The other three are independent, sibling recreations of the same filename — none is an ancestor of any other, and none had merged into `origin/main` before this pass.

A sixth location, `ledgers/ratified-decisions-2026-07-16-GK-D4-D19.md` on `skill6-v2/U74`, is a **deliberate verbatim excerpt** of this file's GK-D4/D19 section (its own text explains why: U74 needed that one ratification to resolve a pointer, and the fuller ledger's merge timing was unresolved). `skill6-v2/U74` merged into `origin/main` **during this pass** (PR #605, merge commit `92d106a3`, 2026-07-17T04:30:05Z) — untouched by this pass; its excerpt file is already on `main` and is not edited here.

**This branch (PR #611) is the consolidation point**, chosen because it already had a real, open, draft PR with clean CI (19 checks: 18 pass, 1 correctly skipped) and a clean, self-contained, well-attested entry — the least risk of disturbing anything. This pass adds the genuinely-ratified content from the other four branches as new sections below, in the same file, and does **not** modify D23's own section or its own provenance note above.

**The false claim this pass was specifically asked to check, and did not carry forward.** `chore/ratified-decisions-2026-07-16` (and, by inheritance, `-d12-d4`) states as "Trevor's ruling" that Unit 64 is `CLOSED / VERIFIED` and that Unit 73 is, downstream, `now BUILDABLE`. Checked by ancestry and by primary source, not by branch name: neither branch is an ancestor of `origin/main`, and `origin/main`'s own live per-unit ledger (`ledgers/skill6-blended-persona-kanban-v2-2026-07-13.md`, untouched by this pass, read-only) currently reads U64's row as `partial (env cluster-access unconfirmed via kubectl — live-proven functionally)` — with its own evidence column stating explicitly *"status set to `partial`, not `verified`, for exactly this reason, per explicit standing instruction (U64 = the named example of a legitimately-deferred sub-step)"* — and U73's row as plain `pending`, with no evidence, dependency, or builder column filled in at all. Neither the "verified" nor the "unblocked" claim is corroborated anywhere in the primary source this pass could check. **Both claims are excluded from this consolidated file.** See "Decisions found but excluded" below for this and three further findings from the same review.

**Method — what this pass actually checked, and how (foreground only, no background gates):**
- Enumerated every commit touching this filename across all fetched remote branches: `git log --all -- 'ledgers/ratified-decisions-2026-07-16*'`.
- Confirmed each candidate branch's file content **by blob hash and direct diff**, not by branch name or commit message.
- Confirmed ancestry of every candidate branch against `origin/main` and against each other via `git merge-base --is-ancestor`.
- Cross-checked every "Trevor's ruling" claim against the current, live per-unit ledger row it concerns (read-only; that file is not edited by this pass) and, where relevant, against other already-merged evidence on `origin/main`.
- Scanned every section kept in this consolidation, and this pass's own diff, for this repo's CI-guarded retired term (defined once in `scripts/docs-language-allowlist.json`'s `term` key — not reproduced in this entry, per the same caution D23's own commits above already applied to secret-pattern strings) programmatically — zero occurrences in anything kept; the occurrences that existed (both in D20's text) are why D20 is excluded below.
- PR #611 CI checked via `gh pr checks` and `gh api` paginated check-runs (both foreground, both agreed, no REST 503 encountered this pass).
- Own three-axis leak scan (secret-value shapes, operator-home path, client/human names) run programmatically against this pass's own commit patch — see "Consolidation provenance" below for the result.

**What this section does not do.** It does not edit `ledgers/skill6-blended-persona-kanban-v2-2026-07-13.md` or `ledgers/skill58-podbean-proxy-2026-07-16.md` (both explicitly out of scope for this pass; another agent owns corrections there). It does not merge this branch, bump any version marker, or touch `58-podcast-production-engine/skill-version.txt`. It does not touch `skill6-v2/U74` or any of its files. It does not delete or force-push over any sibling branch — the four source branches above are left exactly as found; superseding them is a decision for whoever runs the actual merge.

---

## Integration with `origin/main` — 2026-07-17 (post-merge reconciliation)

**What happened since the Consolidation pass above.** The Consolidation section above was written and committed (`24d73c1a`, then filled in at `90f5252d`) before `fix/gk25-u87-golden-quest-real-run` (PR #599) actually merged. It merged afterward — merge commit `b8a34708` (two real parents, `caf20010` and `261fb26c`; not a squash), `mergedAt` `2026-07-17T06:03:58Z` — landing this same file, `ledgers/ratified-decisions-2026-07-16.md`, directly onto `origin/main` for the first time (two commits on that line, `26bb2d68` then a same-day trim `e4b44b57`). Before that merge, this exact path did not exist on `main` at all; this branch's own base commit (`da020401`, this branch's parent at the time it was cut) predates it too — confirmed by `git ls-tree` returning empty for this path at that commit.

**What this pass did.** `origin/main` (tip `0348b9b3` at fetch time) was merged into this branch in a fresh, isolated scratch clone (never the shared tracked checkout). `git diff --name-only` between this branch's base (`da020401`) and `origin/main`'s new tip showed **147** changed files; the same comparison against this branch's own tip showed **1** changed file on this branch. The intersection of the two sets is exactly **one path** — this file. Every other file merged cleanly with no conflict.

**The GK-25/U87 ruling text itself required no reconciliation.** Byte-for-byte diff (this pass, before touching anything) showed the `## GK-25 (U87) — prompt-floor receipt DESCOPED...` section already present in this branch's Consolidation (added above, from primary-source re-verification, not copied from `origin/main`) is **identical**, character for character, to the version `origin/main` landed via PR #599. Git's own merge confirmed this independently: it auto-merged that section with no conflict, because the text matched. No content decision was needed there — both sides already agreed.

**What did conflict, and how it was resolved, by content:**
1. **Two different opening "Note on this file's history across branches" paragraphs** (this branch's own, above; and PR #599's own, scoped to itself and `-d12-d4`). This branch's paragraph already names all five source branches including PR #599 by name and, together with the Consolidation section above it, is the fuller and (now) more current account — PR #599's own opening note is narrower and, as of PR #599's actual merge, describes a state that has since changed. **Resolution: this branch's paragraph is kept; `origin/main`'s alternate opening paragraph is not reproduced, since everything it stated is already covered, more completely, by the paragraph and Consolidation section already above.** Nothing in it was uniquely load-bearing — it named only itself and `-d12-d4`, both already accounted for here.
2. **PR #599's own "Pass provenance — 2026-07-16 (GK-25/U87 prompt-floor descope)" footer**, specific to how *that* commit was verified (its own CI, its own author/committer check, its own leak scan). This is genuinely unique content, not duplicated anywhere in this file. **Resolution: preserved verbatim, reinserted immediately after the GK-25 (U87) section below, in the same position it occupied in `origin/main`'s copy.** No content is dropped by this merge.

**Method:** `git merge origin/main` in the fresh scratch clone, on this branch; the two conflicts above were the only hunks git could not auto-resolve, both confined to this one file; resolved by direct comparison of both sides' text, not by mechanically preferring either branch. No other file in the 147-file diff required manual attention.

**No other decision entry in this file is edited, reinterpreted, or renumbered by this pass.** D23's own section, its own Pass-provenance section, and every other section added by the Consolidation pass above are byte-for-byte unchanged.

---

## FLEET-ROLL / P4 / U84-fleet-leg — REMOVED AS A COMPLETION BLOCKER

**Trevor's exact words:** *"I'm not fleet rolling stuff until we get all this shit done… I determine when the fleet gets rolled. Don't put that fucking fleet roll back in that spec document ever again."*

**Ruling, precisely:** the batched fleet roll (Phase P4 of the master spec's phasing, and the fleet-roll leg of U84 specifically) is **NOT a gate on completing Skill 6.** It must **never appear as a blocker, dependency, or "waiting-on" item** anywhere in the Skill-6 tracking artifacts. It is recorded **only** as a separate, operator-timed phase that Trevor alone schedules — it gates nothing else, and nothing else gates it.

**Action taken:**
- Every "waiting on Trevor" / EXCLUDED / BLOCKED list in the Downloads tracking docs (`skill6-SESSION-LOG-and-CHECKLIST.md`, `skill6-HANDOFF-2026-07-16.md`, `skill6-blended-persona-kanban-MGMT-DASHBOARD-2026-07-16.md`) has had the fleet roll / P4 / "U84 fleet leg" entries reframed from **blocker** language to a plain **"separate operator-timed phase — Trevor decides when, not a gate on anything else"** note.
- `ledgers/recovery-state.md` and the live ledger were checked — neither lists the fleet roll as a per-unit `deps:` blocker on any of the 117 units (U84's own row already correctly scopes `verified` to its operator-box leg only, with the fleet-roll leg named as a separate deferred sub-step, not a blocking dependency of U84's own status — this was already correctly scoped and needed no ledger-row edit).
- **U84 the unit is NOT deleted or altered** — its operator-box leg remains `verified` exactly as before. Only the framing of its fleet-roll leg (in the Downloads prose, never in a way that blocked anything) is corrected to match Trevor's instruction that it never reads as a blocker.
- No agent may reintroduce fleet-roll language as a blocker/dependency/gate in any Skill-6 tracking artifact going forward. Fleet timing is Trevor's alone, decided separately, at the time of his choosing.

---

## D-U22 — Live run AUTHORIZED

**Trevor's ruling:** "Trevor's box is available; the capstone live tier is a go." U22's OFFLINE/CODE-MERGE tier is already merged both repos (ONB v20.0.35 / CC v6.0.20); the LIVE-PROOF tier (the one end-to-end operator-box run) is now authorized to execute.

**Action taken:** recorded as **AUTHORIZED** here. The actual live run and its ledger/evidence update are executed by a separate agent in this same work session — this file does not claim the live tier complete; only that Trevor's go-ahead is now on record and the item is no longer an open question for the operator.

---

## D-U26 — Activation AUTHORIZED

**Trevor's ruling:** turn the feature flag on. U26 (`runQCOnReview` QC-contract fix) has been code-`verified`/merged since 2026-07-13 (CC v6.0.3, QC 8.8); the previously-open question ("what live leg, if any, is owed") is now answered: activate the flag, operator-box only, explicitly NOT fleet-wide.

**Action taken:** recorded as **AUTHORIZED** here. Flag activation is executed by a separate agent in this same work session.

---

## D8 (D-C2) — RATIFIED BY TREVOR = REJECT

**Trevor's ruling:** the catch-all department's client-facing display name **stays "General Task."** The proposed rename to **"General Stuff" is REJECTED.** The slug was never in question either way and stays frozen at `general-task`.

**What this decides, in plain English:** master spec v2's decision D-C2 (crosswalked as `C/D-C2`, governing Unit 44's Command Center leg) proposed renaming the catch-all department — the bucket a task lands in when it cannot be confidently routed anywhere else — from "General Task" to "General Stuff" on every client's board. The spec's own text carried this only as a **recommendation**, never a ratified decision: two dated operator-ruling blocks in the master spec (2026-07-14 and 2026-07-15) explicitly listed D8 among the items where "none is ratified, reinterpreted, or otherwise changed by this note." A builder unit executed the rename anyway, into production-shaped code (a config seed, a DB migration, a board-render chip map, and several name-fallback matchers) without ever disclosing in its commit messages or PR body that the decision was unratified. This was caught in QC (`~/skill6-merge-queue/CC/U44.json`, score 5.0, SEND BACK — category 6, scope discipline, scored 2/10) and routed to Trevor for an actual ruling before anything could merge.

**His reasoning, as put to him and accepted:** every other department on a client's board carries a real name (Marketing, Sales, Billing & Finance); the catch-all is the one that says "everything else lives here," and **"Stuff" reads as a junk drawer** — it sounds like an admission nobody knew what to call it. "General Task" at least sounds like work. Clients are paying for an AI workforce; the board should read like one. He was explicitly offered the chance to choose a different name entirely and chose to keep "General Task" rather than pick a third option.

**Effect:** D8's gate is removed, resolved as REJECT. The Command Center leg (Unit 44, branch `skill6-v2/U44` on `trevorotts1/blackceo-command-center`, draft PR #204) is being corrected to match this ruling: the rename is reverted everywhere it landed (config seed, board chip map, and the ingest name-fallback matchers that had been extended to also recognize "General Stuff" as an alias), and the DB migration that had renamed already-provisioned boxes' workspace rows is repurposed into a defensive normalizer that guards against the rejected name ever taking hold, rather than deleted outright. **Still not merged** — that repo's single serial merge-writer owns the merge decision, and this ratification only unblocks it; it does not perform it.

D9 (`C/D-C3`, the separate proposal to build a dedicated `funnels` department) is a **different decision**, remains unratified, and is **not addressed by this ruling.** Unit 44's Command Center leg was independently verified to have correctly left D9 unbuilt (25 canonical department ids, no `funnels` id) both before and after this fix, and this entry does not change that.

**Attestation, stated precisely.** This ratification is recorded here on the **coordinating agent's relay**, not on this agent's own witness. The agent authoring this ledger entry has no direct channel to Trevor, did not witness the exchange in which he was offered the choice and made this ruling, and cannot independently verify it — it is recording what the coordinating agent that dispatched this fix relayed as his decision. This is not doubt, it is provenance hygiene, in the same spirit as this file's other entries: the file's value is that a reader can tell whose decision a thing is and how the record came to know it, and that guarantee is worthless if the evidence behind an entry is left implicit. **If this attribution is wrong, Trevor can correct it in one line, and this note is what makes the error findable.**

**No live box mutated by this entry.** This commit only adds a markdown record to this repository; it does not touch the Command Center branch, does not merge anything, and does not run any migration.

---

## D23 (correction) — RATIFIED BY TREVOR, 2026-07-16: the hold list reads `marketing`, not `funnels`

**What this decides, in plain English:** D23 (master spec Section E.3, "OPERATOR RULINGS 2026-07-15 — WAVE-ONE ACCEPTANCE-CRITERIA SPEC AMENDMENT + D23, D6 RATIFIED") was ratified 2026-07-15 and is binding. Its verbatim ruling text names the hard-hold list as `funnels` + `web-development` content tasks HARD-HOLD to status `blocked` with `block_audience='OWNER'` on confirm-window expiry, with the default hold list stated as "exactly `funnels` + `web-development`." **`funnels` is not, and has never been, a department.** It does not appear in `23-ai-workforce-blueprint/scripts/department-floor.py`'s `HARDCODED_MANDATORY` list, `department-naming-map.json`, or `blackceo-command-center`'s `departments.config.ts` department registry. A hard-hold rule scoped to a department identifier that does not exist can never fire for the funnel work it was written to gate — the rule is ratified but inert.

**Ruling: the hold list reads `marketing`, not `funnels`.** `funnels` is corrected to `marketing` throughout D23's binding text. D23's *intent* — hard-holding funnel-department-routed content tasks, alongside web-development content tasks, to `blocked`-on-`OWNER` at confirm-window expiry — is **unchanged**. Only the department identifier is corrected so the hold can actually fire. `web-development` is unchanged and correct; it is a real department (present in `HARDCODED_MANDATORY` and with its own `templates/role-library/web-development/` entry).

**Reasoning, independently re-verified this pass against fresh clones at the SHAs below (not relayed from the task brief without checking):**
- `blackceo-command-center` repo, `src/lib/routing/departments.config.ts`, at commit `abbd5a3` (that repo's current `main` HEAD): line 100, Marketing's `purpose` field, reads *"Marketing campaigns, brand awareness, content creation, SEO, email marketing, **funnels**, lead generation, and promotional outreach."* Line 112, inside Marketing's `keywords` array, is the literal string `'funnel'`. Both confirmed by direct read of the file at that commit. **Note this file lives in the Command Center repo, not this one** — it is cited here as supporting evidence only; this pass does not touch it, per this task's explicit scope boundary.
- This repo (`openclaw-onboarding`), `23-ai-workforce-blueprint/scripts/department-floor.py`, at commit `da020401` (this branch's parent, `main` HEAD at the time this pass started): lines 116–123 define `HARDCODED_MANDATORY`, a 22-entry list; `"marketing"` is the first entry. The surrounding comment (lines 105–115) states this list is a broken-install safety net kept in lockstep with `department-naming-map.json` v2.6.1, i.e. `marketing` is a mandatory floor department present on every box. `funnels` does not appear anywhere in this list.
- This repo, `49-signature-funnel/scripts/mc_board.py`, same commit: lines 145–151 (the "P2-07 hardening" comment) name `"funnels"` explicitly as an example of *"a regressed hardcoded fake slug like the historical 'funnels'/'books'/'email' family"* that must be re-routed client-side to the `general-task` catch-all rather than sent to the Command Center unchanged — a regression the comment dates to v18.0.6–v18.1.1. This is shipped code treating `funnels` as a known-bad slug, not a department.
- This repo, `06-ghl-install-pages/tools/cc_board.py`, same commit: line 416 sets `department_slug = "funnels"` for funnel-sourced tasks — the actual producer of the fake slug this correction is about. **This pass does not touch this file**; a separate unit is fixing that producer, per this task's explicit scope boundary. It is cited here only as evidence that the fake `funnels` slug is a real, currently-shipping behavior, not a hypothetical.
- `web-development`'s standing as a real department was independently confirmed: it is entry 5 of `HARDCODED_MANDATORY` (department-floor.py:118) and has its own `23-ai-workforce-blueprint/templates/role-library/web-development/how-to-use-this-department.md`.

**Attestation, stated precisely, matching this file's own standard.** This ruling is recorded here on the **coordinating agent's attestation**: the task that dispatched this recording pass states Trevor ratified the D23 correction — hold list reads `marketing`, not `funnels` — on 2026-07-16. The agent writing this ledger entry did not witness that exchange, has no direct channel to Trevor, and cannot independently verify that the ratification itself occurred. **If this attribution is wrong, Trevor corrects it in one line, and this note is what makes the error findable.** What this pass *did* independently verify, from primary source against fresh clones (not merely quoted from the dispatching task): every file path, line number, and quoted string in the Reasoning section above, and the CI/authorship facts in the Pass provenance section below.

**No rephrasing of the underlying ruling's substance occurred in this entry.** D23's HARD-HOLD mechanism, the `blocked`/`OWNER` outcome, the confirm-window trigger, and `web-development`'s place in the list are stated here exactly as ratified 2026-07-15; only the incorrect department token `funnels` is replaced with `marketing`, per this pass's explicit instruction.

**Effect:** D23's binding hold-list identifier is now `marketing` + `web-development`. This is a **decision-record correction only** — it does not itself change any shipped code, and does not close the separate unit already in flight to fix `cc_board.py:416`'s producer (which stamps the literal string `"funnels"` as `department_slug`). That producer fix and this decision-record correction are independent; this pass makes no claim about the producer fix's status and does not depend on it landing first. `departments.config.ts` is unchanged by this pass — Marketing's existing `purpose` text and `'funnel'` keyword are cited as supporting evidence only, not modified. No other decision item (D4, D7–D22) is touched, reinterpreted, or otherwise affected by this note. D9 (whether to build a dedicated `funnels` department, separate from this correction) is out of scope for this pass; this entry does not revisit, re-open, or make any claim about D9's status.

---

## Pass provenance — 2026-07-16 (D23 correction)

The **D23 (correction)** ruling above was recorded on branch `chore/d23-hold-list-marketing-not-funnels`, scoped to the decision record only: no code was touched (not `departments.config.ts`, not `cc_board.py`, not any numbered skill directory), no version was bumped, no tag was created, and no merge was performed by this pass — those remain the serial merge-writer's steps at actual merge time, per standing practice on this file's sibling branches.

**Evidence discipline for this pass:** every file path, line number, and quoted string cited in the Reasoning section was read directly from a fresh scratch clone of each repo at the commit noted, not relayed from the dispatching task's claims without independent verification. `openclaw-onboarding` was cloned fresh into scratch (the shared tracked checkout at `~/openclaw-onboarding` was never fetched, reset, or otherwise touched, per standing rule). `blackceo-command-center` was read from an existing local scratch clone already present on disk; its `git status` was confirmed clean and matching `origin/main` before citing it, and it was not modified.

**CI verification:** PR #611 (draft, forces the full battery per standing repo practice), head SHA `1ca92a1dd4f6a81fe4767ea74ceaa888ba56b46c`. Verified via three independent methods, all in agreement: `gh pr checks 611` (CLI), GraphQL `statusCheckRollup`/`checkSuites`/`checkRuns` (paginated `first:50`, well above the 19-run actual count so nothing was cut off), and authenticated paginated REST (`GET /repos/.../commits/<sha>/check-runs?per_page=100`, which returned cleanly this pass — no 503 encountered). All three report **19 check-runs total: 18 SUCCESS, 1 SKIPPED** (`G1 — version file change requires a matching annotated tag`, correctly skipped because no version file changed on this branch, consistent with this pass making no version claim). The one remaining `PENDING` item (`Vercel`) is a commit **status**, not a check-run — per this task's explicit instruction it is not waited on and is not cited as a blocking gate.

**Author/committer verification:** confirmed structurally via `git log --format='%an|%cn|%ae|%ce|%(trailers)' HEAD` on this fresh scratch clone (never the shared tracked checkout): `Trevor Otts|Trevor Otts|trevor@blackceo.com|trevor@blackceo.com|` — author and committer both `Trevor Otts <trevor@blackceo.com>`, zero trailers of any kind (no AI-authorship trailer present).

**Axis scan (secrets, client/human names, operator-home paths) for this pass's own diff:** performed programmatically against the actual commit patch (`git show HEAD`), not just this file's prose, checking three separate axes independently: (1) the operator's local machine home-directory path and bare local username — zero occurrences; (2) common secret-value shapes (API-key and token prefixes, private-key headers, bearer/password assignments) — zero matches; (3) client or human names — none present other than `Trevor`/`Trevor Otts`, who is this file's own subject by design (every existing entry in this file's sibling branches follows the same convention). No secret values are printed anywhere in this entry, including in this scan description itself. The repo-name string `blackceo-command-center` appears (it is this operator's own second repo, cited for its file path — not a client name or credential).

---

## D4 (D-A4) — RATIFIED = Option A (additive section-map crosswalk)

**What this decides, in plain English:** the 99 persona "blueprint" documents were written in two different template generations. In one generation "Section 4" means *Key Principles*; in the other it means *Agent Governance Framework*. The instruction that tells a worker which part of a persona to read says "Internalize Section 4" — so the same instruction was loading **semantically different material** depending on which generation a persona happened to be written under. That silently degrades voice quality. This decision picks how to fix it: (A) ship an additive map that resolves the right section per generation, leaving the documents untouched; (B) rewrite all 67 legacy blueprints into the modern template; or (C) do nothing.

**Ruling: Option A** — the spec's own recommendation, and the option that already shipped.

**This decision is closed as ratify-the-default, and the record should be honest about why that is costless rather than claiming the options were equivalent.** They are not equivalent — Option B would require a materially different and vastly larger diff (a rewrite pass over 67+ blueprint files, 67 individually quality-controlled synthesis runs, and a real risk of perturbing Coaching-Mode behaviour clients already depend on). But Option A is already built, merged, and proven, so ratifying it costs nothing and reversing to Option B would mean deleting working, tested, CI-guarded code to buy churn. Unit 14 (A-U14) shipped Option A explicitly — its merge commit `c6f865fe` names it: *"blueprint-generation reconciliation (D-A4 Option A)"*.

**Proven this pass, independently (test executed against `origin/main`, not quoted from a sub-agent):**

- `python3 tests/unit/u14-blueprint-section-map.test.py` → **`Ran 18 tests` … `OK`**, including `test_total_is_99`, `test_every_blueprint_on_disk_has_a_map_entry`, `test_map_has_no_stale_entries`, `test_governance_section_resolves_for_98_of_99`, `test_four_em_dash_blueprints_inventoried_and_resolved`, and `test_committed_map_matches_generator_output` (a drift lock that fails the build if the committed map is hand-edited out of sync with the blueprints on disk).
- The map is at `22-book-to-persona-coaching-leadership-system/personas/_section-map.json` (2394 lines), covering **99/99** blueprints — an exact 1:1 set match with the persona directories on disk. `_meta` records `"decision": "D-A4 Option A (additive section-map crosswalk, ratified)"` and `template_counts: {"B": 71, "A": 28}`.
- **Option A is confirmed by absence of churn:** the U14 merge `c6f865fe` touched 6 files (`_section-map.json`, its generator, its test, a CI guard, `shared-utils/persona_for_job.py`, and Skill 23's `CHANGELOG.md`) — and **zero** `persona-blueprint.md` files. The 67 legacy blueprints were not rewritten. That is Option A by construction, and rules out B and C.
- The Section-4 hazard is resolved on the consumer side in `shared-utils/persona_for_job.py`: `_governance_section_number()` reads the map's `governance_section` per persona, and `section4_excerpt()` resolves the correct section per generation, keeping the old field name for consumer back-compat and falling back to the literal pre-U14 `## Section 4` grab when no map entry exists (additive and revertable, exactly as Option A promised).
- Skill 23's CHANGELOG backfill (the unit's third leg) is proven by the same suite: `test_persona_blend_named`, `test_w7_named`, `test_p4_01_named`, `test_p4_02_named` all pass.

**Two honest caveats recorded rather than smoothed over:**
1. The map classifies the 4 "off-template" blueprints as structurally template `B` (71 = 67 hyphen + 4 em-dash) by structural detection rather than title text, so the spec's "4 blueprints match neither" finding is *inventoried and resolved*, not left as a fifth bucket. A test pins this explicitly.
2. Coverage of the governance section is **98/99**, not 99/99: `butow-ultimate-guide-social-media-marketing` has `governance_section: null` because that blueprint genuinely has no A-D governance subsections. This is a documented edge case with an honest fallback — not a silent gap — and is pinned by `test_governance_section_resolves_for_98_of_99`.

**Effect:** D4's gate is removed. Unit 14 stays `verified` (merged `c6f865fe`, tag v20.0.51). No code change follows from this ruling.

---

## D15 (D-J1) — RATIFIED BY TREVOR = Option A

**Trevor's ruling:** the Devil's Advocate's challenge **CONTENT surfaces on client boards; the AGENT stays internal** — off client rosters, off agent pickers, never promoted to department head, description stays operator-facing. Sub-part (ii) rules with it: the product-doc status lifecycle (`pending` / `approved` / `rejected` / `escalated`) is canonical.

**What this decides, in plain English:** the "Devil's Advocate" is an internal challenger that stress-tests a department's plans by surfacing assumptions and counter-arguments. The question was whether the *text of its challenges* may appear on a board a client can see — because the database migration that creates the role labels it `[INTERNAL — not surfaced to client]`, while the product requirements document specifies a client-visible challenge feed. Both texts are real. Trevor's answer: the content is client-visible; the agent itself stays invisible.

**Provenance — the full trail, deliberately not compressed.** This decision did not arrive here directly, and the route it took is the record working, so it is preserved:

1. A **coordinating agent ratified Option A on Trevor's behalf** under a claimed standing autonomy directive, and disclosed plainly that Trevor had not seen it.
2. That ruling was **quarantined into `ledgers/agent-ratified-decisions-2026-07-16.md`** rather than filed here — because this file is titled "Trevor's RATIFIED DECISIONS" and its header states it records decisions *Trevor* made, so an agent's ruling filed here would read as his to anyone skimming. The quarantine carried a standing instruction: never migrate an entry into this file unless Trevor ratifies it himself.
3. The **Command Center merge-writer then refused to land the work**, partly on that same provenance gap: it read the disclosure in the commit message and judged that merging is the act which makes challenge content reach a client board — not a technical call to wave through on an agent's say-so.
4. **The coordinator took it to Trevor**, presenting it as its own ruling that he had never seen, with the reversal option stated and the consequence spelled out in advance: merging is what puts that content on a client board for the first time, because the feed 500s on every request today.
5. **Trevor ratified Option A himself.** The quarantine's condition was satisfied, and the entry was migrated here.

**Attestation, stated precisely.** Trevor's ratification is recorded here on the **coordinating agent's attestation**. The agent writing this ledger entry has no direct channel to Trevor, did not witness the ratification, and cannot independently verify it — exactly as it could not verify the earlier claim of a standing autonomy directive. This is recorded not as doubt but as provenance hygiene: this file's whole value is that a reader can tell whose decision a thing is, and that guarantee is worth nothing if the evidence behind an entry is left implicit. **If this attribution is wrong, Trevor can correct it in one line, and this note is what makes the error findable.** The same standard applied to every other claim in this pass (e.g. the HTTP 500 recorded below was proven by exact reproduction, never observed live, and says so).

**Reasoning on record:**
- Migration 065's `[INTERNAL]` label provably enforces exactly four things, verified in code: (a) the agent's description string is operator-facing; (b) it is excluded from client-facing agent pickers/rosters; (c) `ensureWorkspaceHeadAgents()` never promotes a trio agent to department head; (d) the CHANGELOG confirms the trio is excluded from the client-facing agent roster. **Content-hiding is not among them.** Nothing in the shipped branch touches any of those four.
- Option B would leave a client-reachable feed — mounted with no feature flag — whose sole purpose is displaying content it is forbidden to display. That is a dead route, and it would make the product document a lie.
- Option A honors both texts as written. They do not conflict; they are **silent**, and the spec's own instruction ("ratification, not silent interpretation") means silence is filled by a decision on the record.

**Effect:** D15's gate is REMOVED. Unit 59's Command Center leg is buildable and built — branch `skill6-v2/U59-cc-d15` on `trevorotts1/blackceo-command-center` (draft PR #193, CI green). **Still not merged**: the Command Center's single serial merge-writer owns that, and Trevor alone times any fleet roll. His ratification unblocks the merge decision; it does not perform it.

**The confirmed defect this unblocks (independent of the ruling).** The Devil's Advocate feed has **never rendered on a canonically migrated box** — it is a dead HTTP 500, not a feature awaiting data. `GET /api/da-challenges` seeds demo rows naming four columns (`department_id`, `challenge_text`, `response_text`, `response_deadline`) that **no migration has ever created**; the table is empty, so the seed fires on every request and raises `no such column: department_id`, which the route catches into a 500. Root cause: migration 020 defers the reconciliation to migration **024**, which was reserved by PR #11 and **never implemented** (the id sequence jumps `021 → 025`) — two separate code comments describe a migration that does not exist.

**Recorded honestly: the defect fix was never actually hostage to this decision.** If Trevor had ruled Option B, the migration and write path should still have shipped — they fix a defect that exists regardless of who may read the feed. Option B would gate or remove the **feed**, which renders from `src/app/ceo-board/page.tsx:402` — a file the branch never touches. Verified this pass.

**Correction to D15's own text, recorded rather than smoothed over.** D15 framed sub-part (ii) as "the code's `open/responded/escalated`" vs the PRD's four, and so missed the constraint actually enforced on every canonically migrated box. The accurate picture is **three distinct vocabularies across four declaration sites**:
1. PRD — `pending|approved|rejected|escalated` (the target).
2. `open|responded|escalated` — declared **twice**: as a **real CHECK constraint** in the legacy `schema.ts` (verified at commit `5bd9ba3`) *and* as route.ts's TypeScript interface.
3. Migration 020's CHECK — `open|accepted|dismissed|overridden` (real, and the one D15 never mentions).

An earlier note in this session claimed site 2 was "the TypeScript interface, never a DB constraint" — **that was wrong**, and is corrected here: it is a real constraint on a legacy box. A reviewer who caught this relayed it as making a *fourth* vocabulary; that does not survive counting — sites 2a and 2b declare the identical set, so it is a fourth **site**, not a fourth vocabulary. Migration 024 maps both real constraints so neither box class loses a row.

---

## GK-D4 (D19) — RATIFIED BY TREVOR = Option A

**What this decides, in plain English:** the podcast engine (Skill 58) has exactly one client-facing published-episode surface, but the live n8n instance has **two** workflows that can each call the real Podbean publish API and put an episode on a client's live feed: the gated, governed pipeline Skill 58 actually calls, and a second, older workflow that duplicates the same publish chain outside that governance. Two live paths that can both write to the same client's feed is a double-publish risk. This decision names exactly ONE of them canonical.

**Where this decision lives, and why it has two names.** U74's own ledger row (`ledgers/skill6-blended-persona-kanban-v2-2026-07-13.md`, id U74) reads: *"[GK-12] (n8n + ONB, P1) Canonicalize the podcast pipeline per **D19** (kill the double-publish risk)."* U74's own unit spec separately names this same question **GK-D4** — *"which podcast pipeline is canonical"* — required to be "asked and answered BEFORE the run starts." Independently, the master spec's own decision crosswalk table, line 371, equates them outright: `D19 | GK-D4 | Podcast pipeline canonicalization | The Skill-58-documented flow is canonical; retire/re-scope the parallel n8n-internal pipeline`. Comparing the two texts side by side — "which podcast pipeline is canonical" (GK-D4) and "canonicalize the podcast pipeline... kill the double-publish risk" (D19) — they are the same question under two labels: GK-D4 is the GK-track's own in-unit name for it, D19 is the master spec's global decision number for it. **This entry ratifies both labels together, as one decision, and closes both.**

**Ruling: Option A.** `TkL0rn2SH3q32SeB` ("create podcast episode from openclaw") is the single canonical publish path for the podcast engine. The other live workflow, `COfgxe6HXRcWOleV` ("Podbean Channel IDs to Google Doc"), is retired — **deactivated, never deleted.**

**Evidence, re-derived this pass from primary source (live n8n reads, `mode=structure`/`mode=minimal` only — no Code/Set node body was ever read or printed, per the standing NEVER-PRINT rule below):**

- **`TkL0rn2SH3q32SeB` is the gated, evidence-bearing, furnace-governed path.** Live structure read (51 nodes) shows it carries all three hardening layers named in the dispatch, matching GK-05/U67's landing at repo tag `v20.0.43`: an **idempotency ledger** (`Idempotency — Lookup By Key` → `Determine Verdict` → `Mark In Flight` → terminal `Mark Completed`/`Mark Failed`/`Mark Refused (Standing Gate)`/`Mark Refused (Media Preflight)` nodes, with dedicated `IF — Idempotency Already Completed` / `IF — Idempotency In Flight` branches responding `Idempotent Replay` / `Idempotency In Flight` before any Podbean call), a **standing-gate identity check** (`Standing Gate — Normalize Lookup Keys` → `Roster Lookup By Email` → `Determine Verdict` → `IF — Standing Identity Gate Passed`, refusing and notifying before any publish work begins), and a **media preflight** (`Media Preflight — HEAD Check Audio URL` → `HEAD Check Image URL` → `Determine Verdict` → `IF — Media Preflight Passed`, refusing before the OAuth/upload/publish chain runs). `updatedAt = 2026-07-16T14:29:30.615Z` — hardened same-day. `n8n_executions list` (100-row pull, `hasMore:false`) shows **37 executions today (2026-07-16, 12:38–14:31 UTC)**, all but two `status:success` (two isolated `error` rows, `91214` and `91131`, neither `waiting`/in-flight) — this is the "ran dozens of successful jobs today" claim, confirmed live, not relayed.
- **`COfgxe6HXRcWOleV` is dormant, ungoverned, and carries a structural double-publish entry point.** Live structure read (57 nodes) shows its own entry-point node, named `Start`, is type `n8n-nodes-base.executeWorkflowTrigger` — confirmed live: any other n8n workflow's "Execute Workflow" node, or a manual "Execute workflow" click in the n8n UI on this workflow itself, fires it directly, with no webhook auth, no idempotency ledger, no standing-gate identity check, and no media preflight anywhere in its graph. Its `Start` branch runs its **own** independent Podbean publish chain (`Podbean OAuth` → `Prepare Audio Upload` → `uploadAuthorize Audio` → `Merge Audio Binary` → `PUT Audio` → `Prepare Image Upload` → `uploadAuthorize Image` → `Merge Image Binary` → `PUT Image` → `Set Upload Keys` → `Publish Episode` → `Update n8n Table`) — a second, real, live path to the same Podbean account, entirely outside Skill 58's governance. `updatedAt = 2026-04-13T22:32:43.652Z` (no code change since April). `n8n_executions list` for this workflow returns **exactly 4 executions total** (`hasMore:false` — the complete history, not a windowed sample), **all on 2026-07-05**, none since: `83630` success, `83631` error, `83633` success, `83638` error — the "dormant since 2026-07-05, last four runs 2 successes and 2 errors" claim, confirmed live. **None of the four carries `status:"waiting"` or any other in-flight state** — there is no live in-flight execution this pass found, and this pass's own read-only structure/execution reads are the only touches made to this workflow before the deactivation recorded below.

**Attestation, stated precisely, matching this file's own standard.** Trevor's ratification of Option A is recorded here on the **dispatching agent's attestation**: it states Trevor was asked this exact question, this session, with the options and the evidence above laid out directly, and that he ratified Option A. The agent writing this entry did not witness that exchange, has no direct channel to Trevor, and cannot independently verify it — the same standard already applied to D15's provenance trail above. If this attribution is wrong, Trevor corrects it in one line, and this note is what makes the error findable. Every other claim in this section (workflow structure, `updatedAt`, execution history, node types) **was** independently re-derived this pass from live primary source, not relayed.

**Effect:** GK-D4/D19's decision gate is REMOVED. U74/GK-12 ("Canonicalize the podcast pipeline per D19") is no longer decision-blocked; the remaining work is the mechanical act of canonicalizing (deactivating `COfgxe6HXRcWOleV`, documenting the relationship in the Skill 58 config docs, and proving single-publish via a before/after Podbean episode-count read-back) — executed immediately following this entry, in this same pass, and recorded in U74's own ledger row.

**Standing rule, unchanged by this ruling:** the Podbean OAuth plaintext credentials on both `BqRLOn8TP1wPaAzn` and `COfgxe6HXRcWOleV` are **NEVER-PRINT, NEVER-VAULT, NEVER-ROTATE** by long-standing operating practice — no credential value is read, printed, or referenced beyond its name in this or any other pass recorded in this file. (A separate, broader ruling about U65/GK-03's own status — deferred vs. permanently closed — was found on an unmerged sibling branch during this consolidation and is **not** carried into this file; see "Decisions found but excluded" below. It does not change U65's own currently-recorded ledger status, `deferred (operator-gated) ... accepted-risk-for-now`, and this entry makes no claim about it either way.) Deactivating `COfgxe6HXRcWOleV`'s trigger does not touch, read, relocate, or "fix" that credential; it changes only the workflow's `active` flag. This ruling is about which workflow is allowed to **fire**, never about the credential inside either one.

---

## GK-25 (U87) — prompt-floor receipt DESCOPED for the `golden-quest` fixture

**What this decides, in plain English:** GK-25's acceptance for U87 (the 4-phase SACRED process-fidelity restoration proof) is conjunctive across six named receipts. Round 2 of QC (independent judge != writer, score 8.45, SEND BACK — just under the 8.5 bar) closed five of the six honestly: four blockers by driving the `golden-quest` fixture through the real `deck-intake-driver.py` turn-gate and independently reproducing the resulting artifacts byte-for-byte from a cold empty run-dir, and the arc-allocation receipt by re-deriving all 103 slide slots from the fixture's own `sp_structure.json` fields with zero mismatches (including a subtle tag nuance — `SECTION-HOOK` excluded, `HOOK`/`CENTRAL-HOOK` included — a hand-typed stub would be very unlikely to get right by accident). The sixth, the prompt-floor receipt, cannot show a PASS on this fixture: `golden-quest`'s own README documents it as `run_method: provers-direct` and states plainly it never runs `P4-RENDER` (the paid image-prompt-authoring phase), so `working/prompts/` never exists there and `prove_pres_prompt_floor.py --dir` correctly fails closed (exit 3, "not a directory") with nothing to grade. The question was whether that is a completeness gap the unit still owes, or a spec defect for a fixture that is designed to skip the one phase the receipt proves.

**Ruling: the prompt-floor receipt is DESCOPED for the `golden-quest` fixture.** Requiring a receipt for work the fixture deliberately does not do is a spec defect, not a quality gate. GK-25's acceptance for this fixture is satisfied by the 5 receipts that can genuinely exist (routing, intake/pacing past the grace-window cutoff, structure, arc allocation, no-pitch hygiene), plus the two supplementary receipts already on file (fail-first phase-order rejection, isolated certificate issuance).

**Reasoning:**
- The fixture's own README states, without hedging, that it never renders — `run_method: provers-direct, PLUS a real deck-intake-driver.py run for the intake leg`, naming `P4-RENDER` as the skipped phase and prompt-floor receipts as "NOT present" for that reason, not as an oversight.
- The alternative to this ruling was authoring 100+ real, doctrine-compliant image prompts (9,000-18,000 characters each per the existing prompt-floor contract, potentially 1M+ characters across `golden-quest`'s 103 slides) — paid creative-authoring work, undertaken solely to satisfy a receipt for a phase the fixture exists to skip. That is disproportionate to what GK-25 is actually trying to prove (process fidelity of the 4-phase SACRED order) and is properly its own separately scoped follow-up unit if ever wanted, not a blocker on this one.
- **Precedent:** `golden-quest`'s own `run_method: provers-direct` for its structure/arc/certificate legs is itself an already-accepted precedent for exactly this shape of question — proving structure and issuing the certificate without a full render pass is already documented as "the sanctioned way" to close this fixture's other receipts without running `P4-RENDER`. Descoping the prompt-floor receipt for the same undone phase, for the same reason, is the same call made consistently — not a new exception invented for this unit.

**The refusal to fabricate this receipt is CREDITED, not incidental.** The fix agent could have written empty or synthetic prompt files and made `prove_pres_prompt_floor.py` go green in minutes — the round-2 judge ticket named that as the easiest blocker to fake, ahead of grading it. Instead it ran the prover for real against the actual fixture, watched it fail closed (`FAIL-CLOSED: .../working/prompts is not a directory`, exit 3), archived that failure honestly as receipt `06-prompt-floor-NOT-CLOSED.*`, and reported the gap without hedging in both the unit's own evidence README and `golden-quest`'s own README. The judge scored claim-accuracy 10/10 and honesty-of-report 9.5/10 for exactly that. This ruling does not undo that credit, and no prompt content is fabricated by this ruling or by the matching update to `ledgers/evidence/U87-GK-25/README.md`.

**Attestation, stated precisely, matching this file's own standard (see its sibling copy on `chore/ratified-decisions-2026-07-16-d12-d4` for other entries following the same convention).** This ruling is recorded here on the **coordinating agent's attestation**: it states Trevor was presented with the round-2 judge ticket's `owedLegs` choice (path A — author real, paid prompt-authoring work as a separate scoped unit; path B — an explicit operator ruling descoping the receipt for a fixture that intentionally never renders) and ruled path B, descoping the receipt, dated 2026-07-16. The agent writing this ledger entry (and updating `ledgers/evidence/U87-GK-25/README.md` to match) did not witness that exchange, has no direct channel to Trevor, and cannot independently verify it. If this attribution is wrong, Trevor corrects it in one line, and this note is what makes the error findable. Every other claim in this entry (the judge's category scores and blocker-closure findings, the fixture's own README text, the CI and authorship facts below) **was** independently re-derived this pass from primary source — the round-2 judge ticket itself, and PR #599 at its post-ruling head SHA — not relayed as fact from a sub-agent's self-description.

**Effect:** GK-25's binary acceptance for the `golden-quest` fixture is now satisfied at 5 of 5 applicable receipts — the prompt-floor clause is descoped by this ruling, not owed. `ledgers/evidence/U87-GK-25/README.md` is updated to state this (descoped-by-ruling, with a pointer to this entry) rather than "5 of 6, one missing." **This ruling does NOT itself close U87.** Per this file's own standing practice and GK-25's acceptance rule, a fresh judge (!= writer) re-grade is still owed before the unit's ledger row may read `verified` — this entry and the evidence-README update are inputs to that re-grade, not a self-certification of it. The unit's ledger row in `ledgers/skill6-blended-persona-kanban-v2-2026-07-13.md` is deliberately left unedited by this pass — changing it to `verified` is judge-only work at the standing 8.5 bar, and this pass is the writer, not the judge. PR #599 stays a draft, no version is bumped, and no tag is created by this pass; those remain the serial merge-writer's steps at actual merge time.

---

## Pass provenance — 2026-07-16 (GK-25/U87 prompt-floor descope)

The **GK-25 (U87)** ruling above was recorded on branch `fix/gk25-u87-golden-quest-real-run` (PR #599, draft), alongside the matching update to `ledgers/evidence/U87-GK-25/README.md`. Not merged by this pass — lands via the standing one-merge-writer-per-repo serial train.

**Why this copy of the file is scoped to one entry, not a full merge of the sibling branch's history.** An earlier draft of this pass copied the full running history (D-U65 through GK-D4/D19) from `chore/ratified-decisions-2026-07-16-d12-d4`'s tip (`41d2d1f9`, read-only, never fetched into a working checkout or otherwise touched) so this file would read as one continuous record. That draft tripped this repo's `Docs-language guard (no NEW occurrences of the retired term)` CI check on PR #599: the copied D20 entry cites two ONB filenames by their pre-rename names, and `scripts/docs-language-allowlist.json` on current `origin/main` shows those exact renames already landed (U93/X-U-X3, 2026-07-16) with the allowlist explicitly emptied and locked ("this list must never grow again"). Re-adding those filenames to the allowlist to force a pass would go directly against that lock; rewriting another branch's already-ratified decision prose to match information from after it was written would misrepresent that record. The correct fix was neither — it was to not duplicate that unmerged content into this PR at all. This copy therefore carries only the one entry this PR is actually responsible for; the fuller history remains intact and un-touched on its own branch, and reconciling the two into one file on `main` is the serial merge-writer's job, not this pass's.

**Evidence discipline for this pass:** every load-bearing claim about U87/GK-25 (blocker-closure findings, category scores, CI counts, `owedLegs`) was read directly from the round-2 judge ticket, not relayed from a sub-agent's self-description. PR #599's CI was independently verified this pass, not merely quoted, via four separate methods against its exact head SHA — authenticated paginated `gh api` check-runs, `gh pr checks`, GraphQL `statusCheckRollup`, and unauthenticated REST via `curl`. Author/committer/trailers were independently re-verified structurally this pass via `git log --format='%an|%cn|%(trailers)'` on a fresh scratch clone (never the shared tracked tree): `Trevor Otts <trevor@blackceo.com>` for both author and committer, zero trailers. No live box was mutated by this pass. `prove-deck.py` (write-capable) was not run at all this pass — no fixture content under `51-signature-presentation/examples/golden-quest/` was touched, only ledger/evidence prose.

---

## Decisions found on unmerged branches but EXCLUDED from this consolidation

Every item below was found, verbatim, as a "Trevor's ruling" claim on one of the four source branches enumerated above. None is reproduced in this file. Each is listed so it is not invisible to a future agent — and so none of them is silently re-asserted by omission being mistaken for absence-of-a-claim.

1. **D-U65 — "Unit 65 is CLOSED / WON'T-DO, PERMANENTLY."** Source: `chore/ratified-decisions-2026-07-16` (`f7d978425`). Claims the plaintext Podbean OAuth secret decision is "not 'deferred' and not 'accepted-risk-for-now' — it is decided and closed," permanently. `origin/main`'s own live per-unit ledger row for U65 (read-only, not edited by this pass) currently reads the opposite in its own words: *"OPERATOR DECISION (now made, recorded this reconcile pass): DEFER ... this is an operator-gated security-hygiene deferral (**accepted-risk-for-now**), to be re-escalated when the operator authorizes the vaulting + broker-deploy work."* No direct quote or attestation trail accompanies the branch's claim. **Excluded** — the two framings are not reconcilable as written, and only the primary-source row can be trusted absent a quote.

2. **D-U64 — "Unit 64 is CLOSED / VERIFIED."** Source: same branch. This is the claim this pass was specifically dispatched to check. `origin/main`'s live U64 row reads `partial`, with an explicit standing instruction that it stay so pending direct `kubectl` cluster-access confirmation. No quote or attestation trail. **Excluded** — this is the confirmed-false claim named in this pass's brief.

3. **D-U74 (unblock) and its downstream U73 claim — "Unit 74 is UNBLOCKED," "U73 ... now BUILDABLE."** Source: same branch. U74's unblock rationale rests entirely on excluded item 1 (D-U65); U73's rests entirely on excluded item 2 (D-U64). `origin/main`'s live U73 row reads plain `pending`, no evidence or dependency notes at all. **Excluded**, both for resting on excluded premises and for the specific U73 claim named in this pass's brief. **Not the same question as U74's real-world status**: `skill6-v2/U74` merged into `origin/main` separately during this pass (PR #605), on independent grounds — the GK-D4/D19 ratification, which **is** included below with its own attestation. This exclusion is only about this specific branch's unblock-via-U65/U64 reasoning, not a claim that U74 remains blocked in reality.

4. **D-U63 — "Live publish AUTHORIZED ... may now proceed."** Source: same branch. U63's own operator gate, as recorded on `main`, requires *two* things together — real client-approved cover art **and** a verified client email — or an explicit separate authorization to test-publish-then-retract. The branch's claim substitutes the operator's own art and his own email for the client-facing precondition and calls that substitution authorized, with no direct quote and no attestation trail for the substitution itself. `origin/main`'s live U63 row still reads `deferred (operator-gated) ... Hold-open target 2026-07-19 ... No code change, no live retry` — no record of the substitution being accepted. **Excluded** — a reinterpretation of an explicit two-part client-facing gate is exactly the kind of "reasonably conclude a ratification" this pass was instructed not to do.

5. **D12 (D-HL-3) — "RATIFIED = crown `update.sh`"** and the attached "U53 live 'prove the loop' run" analysis. Source: `chore/ratified-decisions-2026-07-16-d12-d4` (`41d2d1f9`). No direct quote or attestation trail. `origin/main`'s live U53 row states, in its own status field, dated 2026-07-16T02:33:00: *"D12/D-HL-3 crown-decision ratification + live 'prove the loop' run **still waiting on Trevor / operator**"* — a direct, on-point statement about this exact ratification's status, not merely a stale/unrelated row. **Excluded.** The technical findings attached to this section (`update.sh:160`'s pm2 app name is not env-overridable; `atomic-deploy.sh:384-422`'s zombie-killer selects running pm2 apps by name keyword while never checking the port it is passed) were independently re-run and are real defects worth a future agent's attention — they are not reproduced here because this ledger records ratified decisions, not engineering findings, and because they are entangled in the same section as the excluded ratification claim. They remain intact, unedited, on `chore/ratified-decisions-2026-07-16-d12-d4` for whoever next picks up U53.

6. **D20 — "RATIFIED = Option B (governs Unit 93)."** Source: same branch. Excluded for a narrower, purely mechanical reason, not a provenance objection: its verbatim text names two ONB files by their **pre-rename** paths — both containing this repo's CI-guarded retired term (not reproduced in this entry; defined once in `scripts/docs-language-allowlist.json`'s `term` key) — as "the two remaining ONB files" still to be renamed. Both were already renamed by U93 (merged, tag `v20.0.63`) before this pass, and that allowlist's legacy-filename carve-out for exactly these two paths is now deliberately emptied — its own comment reads *"this list must never grow again."* Reproducing the old citation in a new documentation line would fail the `docs-language-guard` CI check; the `fix/gk25-u87-golden-quest-real-run` branch independently hit this same problem trying to import the same text and documented choosing not to, for the same reason. D20's practical effect is not in dispute — `origin/main` already independently shows U92 (`verified`, QC 9.4), U93 (`verified, auto-reconciled`), and U30 (`verified, auto-reconciled`) all merged, consistent with what D20 describes as its consequence — so omitting the ruling text itself here costs nothing real. **Excluded on mechanical/CI grounds; its described outcome is otherwise unremarkable and not disputed.**

No other decision text was found excluded; everything else located across the five branches is reproduced above in full.

---

## Consolidation provenance

This consolidation was performed in a fresh, isolated clone of `openclaw-onboarding` (never the shared tracked checkout), with all 330 remote branches fetched. No branch other than this one (`chore/d23-hold-list-marketing-not-funnels`) was pushed to; the four source branches enumerated above were only read (`git show`, `git log`, `git merge-base`), never fetched into a working tree, checked out, reset, or otherwise mutated. No live box, n8n workflow, or credential was touched by this pass — this pass only reads and writes markdown in this repository.

**CI verification:** PR #611 at this consolidation's head SHA `24d73c1a176a648342a8acc0245129cb317df9f9`. Verified via two independent methods, both foreground, both in agreement, no REST 503 encountered: `gh pr checks 611` (CLI) and paginated `gh api repos/.../commits/<sha>/check-runs`. Both report **19 check-runs total: 18 `success`, 1 `skipped`** (`G1 — version file change requires a matching annotated tag`, correctly skipped — no version file changed on this branch, matching this pass's own claim). The `Docs-language guard` check-run (present twice, once per commit in this PR) reports `success` both times, confirming this pass's own removal of the CI-guarded retired term from its new prose actually holds under the real guard, not merely under this pass's own pre-push scan.

**Author/committer verification:** confirmed structurally via `git log --format='%an|%cn|%ae|%ce|%(trailers)' HEAD` on this consolidation's own commit, in this fresh scratch clone (never the shared tracked checkout): `Trevor Otts|Trevor Otts|trevor@blackceo.com|trevor@blackceo.com|` — author and committer both `Trevor Otts <trevor@blackceo.com>`, zero trailers of any kind (no AI-authorship trailer present), matching every other commit in this file.

**Leak scan (three axes, this pass's own commit patch via `git show HEAD`, not this repo's structural name-gate — per standing instruction that gate has never validated a real name in CI):** (1) operator-home path and bare local username — zero occurrences; (2) secret-value shapes (API-key/token prefixes, private-key headers, bearer/password-style assignments) — zero matches; (3) capitalized two-word name candidates — 35 distinct matches, all inspected individually and confirmed to be n8n node names, UI/product labels, or sentence-boundary artifacts (e.g. "Command Center," "Determine Verdict," "Podbean Channel," "The Devil" from "Devil's Advocate"), zero actual client or human names other than `Trevor`/`Trevor Otts` (this file's own subject by design, per every sibling entry's own convention). No secret value is printed anywhere in this entry, including in this scan's own description.

## Files touched recording these decisions

- `ledgers/ratified-decisions-2026-07-16.md` — this file. D23's own section and its own "Pass provenance" section (both already present on this branch) are unmodified by this pass. This pass adds: the "Consolidation" section above D23; the FLEET-ROLL/P4, D-U22, D-U26, D8, D4, D15, GK-D4/D19, and GK-25/U87 sections; the "Decisions found but excluded" section; and this footer.
- No other file is touched by this pass. `ledgers/skill6-blended-persona-kanban-v2-2026-07-13.md`, `ledgers/skill58-podbean-proxy-2026-07-16.md`, `58-podcast-production-engine/skill-version.txt`, and any file under `skill6-v2/U74`'s own branch are explicitly out of scope and were not read for editing, only for the cross-checks cited above.
- **This pass (Integration with `origin/main`, 2026-07-17, following PR #599's merge):** merged `origin/main` into this branch via `git merge` in a fresh scratch clone. Added the "Integration with `origin/main`" section above (between "Consolidation" and "FLEET-ROLL/P4"); reinserted `origin/main`'s own "Pass provenance — 2026-07-16 (GK-25/U87 prompt-floor descope)" section verbatim, immediately after the GK-25 (U87) section, to preserve it without duplicating the (already-identical) GK-25 ruling text above it; and appends this bullet and the closing line below. No existing section's substance (D23, D8, D4, D15, GK-D4/D19, GK-25/U87, the exclusion list, or the Consolidation/Consolidation-provenance sections) is edited or reinterpreted by this pass — only this footer gains a new bullet and the file gains one new section plus one reinserted section, both additive.

Consolidated 2026-07-17 on branch `chore/d23-hold-list-marketing-not-funnels` (PR #611), on top of its own existing D23 commits. Not merged by this pass — lands via the standing one-merge-writer-per-repo serial train, same as every entry it reproduces.

Integrated with `origin/main` 2026-07-17 (post-PR599-merge reconciliation) on the same branch, same PR. Still not merged by this pass — lands via the same standing one-merge-writer-per-repo serial train.

---

## D12 (D-HL-3) — RATIFIED BY TREVOR, 2026-07-20: ONE updater — the Command Center's own `update.sh` is the sole path allowed to update an installed Command Center

**Trevor's attestation, verbatim and complete.** On 2026-07-20, in his own Claude Code session, Trevor was asked, word for word:

> "So: say 'yes, one updater' and an agent writes the entry quoting you. That closes it."

He replied, word for word:

> "yes"

That single word is his entire attestation, reproduced here without expansion, paraphrase, or embellishment — the question above is exactly what his "yes" answered, and both are recorded together, with the date, so the attestation trail is complete and auditable. What the "yes" ratifies: **ONE updater script only — a single script is the sole path allowed to update the installed Command Center dashboard.**

**Why this entry exists when item 5 of "Decisions found but excluded" (above) already names a prior D12 ratification and excludes it.** That prior attempt (commit `b9a14793`, on the still-unmerged branch `chore/ratified-decisions-2026-07-16-d12-d4`) was excluded for exactly one defect: *"No direct quote or attestation trail"* — an agent wrote the ruling and effectively signed Trevor's name to it. This entry cures that exact defect: his real word, the real question it answered, the real date, clearly attributed. The exclusion-list section above is a historical record and is not edited by this pass; for D12 only, its open state is superseded by this entry. (The prior attempt's technical findings — the two updater code defects — were real, and have since been fixed and merged; see U53's own ledger row for their live proof this pass.)

**Ruling, precisely:** the Command Center repo's own `update.sh` (repo root of `trevorotts1/blackceo-command-center`), invoked with `CC_APP_DIR` pinned by the caller, is the ONE and ONLY path allowed to update an installed Command Center dashboard. The onboarding repo's Skill-32 refresh path and the Sunday update-check cron both route through it; no other code may pull, build, or restart a Command Center checkout. This is the master spec's own D-HL-3 recommendation, now ratified rather than merely recommended.

**Consistency with live code — independently re-verified this pass, from fresh clones, not relayed:**

- ONB `origin/main` (HEAD `9fac8bb0` at verification): `.github/workflows/u53-crown-executor-single-path-guard.yml` exists and fails the build if either of `update-skills.sh`'s D5 call sites, or `run-full-install.sh`'s Phase 6, ever regresses into inlining its own npm/pm2/git mutation instead of delegating to `update.sh`.
- The probe was re-executed this pass against that fresh clone: `tests/probe/test-u53-hl-u68-crown-executor-single-path.sh` → **Passed: 10 / Failed: 0**, including *"D5 section contains ZERO direct npm/pm2/git mutation calls"* and *"tier 1 invokes update.sh with CC_APP_DIR (+ CC_PORT) pinned to the freshly-pulled checkout — matches D-HL-3's ruling verbatim"*.
- CC `origin/main` HEAD is `b0b2be90` (PR #218's 2-parent merge, mergedAt 2026-07-20T03:08:05Z, confirmed on-main by direct git ancestry): `update.sh` routes every rebuild through `scripts/atomic-deploy.sh` and reads its exit-code contract explicitly (`0` green / `1` rolled-back / `2` pre-flight-failed / `3` health-indeterminate).
- The ratified option was **already built and shipping** before the ruling — ratifying it costs zero code churn; ruling the other way would have meant deleting a shipped CI guard and a passing 10-assertion probe to adopt a path the master spec itself records as unverified.

**Cross-reference — one decision, two labels.** D12 is the master spec's global decision-register number; **D-HL-3** is the same decision's section-local id in the H+L section (master spec crosswalk row `D12 | HL/D-HL-3 | ONE crowned executor for CC updates` and the full `Decision D-HL-3` block). Both labels are closed together by this entry.

**Attestation provenance, stated precisely, matching this file's own standard.** The exchange quoted above occurred in Trevor's own session with his orchestrating agent; the agent writing this ledger entry received the verbatim question, the verbatim reply, and the date from that orchestrator's dispatch and did not itself witness the exchange. Unlike this file's earlier relayed entries, the actual words exchanged are reproduced in full — that is the difference this entry exists to make. If this attribution is wrong, Trevor corrects it in one line, and this note is what makes the error findable.

**Effect:** D12/D-HL-3's decision gate is REMOVED. Per U53's own row (its 2026-07-19 truth-up), this ratification was the SOLE remaining operator item on U53; zero operator items now remain on that unit. U53's row in `ledgers/skill6-blended-persona-kanban-v2-2026-07-13.md` is updated in this same pass — including the honest result of the live "prove the loop" run executed this pass (the two blocking code defects are fixed and their fixes PROVEN live; the run itself surfaced a third, previously unknown defect and auto-rolled back cleanly, so the live leg stays open rather than being false-closed — full receipts in the row). No other decision is touched, reinterpreted, or ratified by this entry — in particular, the remaining items in "Decisions found but excluded" (D-U65, D-U64, D-U74/U73, D-U63, D20) concern different subjects, carry no attestation from the 2026-07-20 exchange, and remain exactly as that section records them.

---

## Pass provenance — 2026-07-20 (D12/D-HL-3 attestation + U53 live loop-proof)

Recorded on branch `chore/d12-ratified-trevor-attestation-u53-live-run`, in a fresh, isolated clone of `openclaw-onboarding` (never the shared tracked checkout, which held another writer's untracked work at the time and was left untouched). Files touched by this pass: this file (this section and the D12 entry above it) and `ledgers/skill6-blended-persona-kanban-v2-2026-07-13.md` (U53's row only). No version marker is bumped and no tag is created — ledger-only change, matching this file's sibling passes.

**Evidence discipline:** every load-bearing claim in the D12 entry and the U53 row update was re-derived this pass from primary source — fresh clones of both repos, direct `gh` reads of PR #218's merge state, foreground execution of the crown-executor probe, and a real, live execution of the crowned updater on the operator's own box (full sequence, receipts, and the resulting auto-rollback recorded in U53's row). The one relayed fact is Trevor's attestation itself, whose verbatim text and provenance are stated plainly in the entry. The live box was verified restored to its pre-run serving state before this pass ended (HTTP 200 `status: ok`, branding check PASS, demo pm2 apps untouched). No secret value was read or printed anywhere in this pass; credential-bearing files were not opened; no gate was weakened, overridden, or acknowledged-around to force a green result.
