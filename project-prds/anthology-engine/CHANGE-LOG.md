# CHANGE LOG, ANTHOLOGY ENGINE
### Append-only record of every artifact change: repo commits, merges, tags, version bumps, doc updates, config changes, legacy workflow changes, credential rotations (by label only, never value). One row per change, newest last. Rows are NEVER edited or deleted; corrections are new rows.
### Doctrine on every row: move in silence; no Anthropic in runtime; credentials by label and location only, never value; Convert and Flow naming in every surface; config writes as the node user; zero client personally identifiable information; only sub-agents build.

## ROW LAW (who appends, and when; binding per WAVE-PLAN global rule 4 and PRD Section 10.2, the incremental persistence law)

1. PER MERGED FEATURE: the MOMENT a feature or slice passes Gate A (the fleet 10-category build/merge rubric, threshold 8.5, scored by an independent QC agent) and is COMMITTED AND PUSHED, the SAME agent appends its row here, in the same breath as ticking CHECKLIST.md, marking TODO.md done, and appending SESSION-LOG.md. Completed-and-pushed work is NEVER lost to a session limit; on any cold resume, rows present here plus unchecked CHECKLIST/TODO items ARE the ground truth of what remains.
2. WAVE 0 rows: the Wave 0 agents record the prompt-ingestion evidence row (W0.2: the four largest prompts ingested from Outline Agent.json, Chapter Re-Writer Agent.json, and Single Chapter Cover Image Gen.json; sha256 pin ids and byte counts only, NEVER prompt text; the 10 [UNCHANGED] placeholders restored from the CSV HTML Book record; word band normalized to 2,000 to 3,500; Make.com variables mapped) and the credential-rotation rows (W0.3: both exposed keys rotated, BY LABEL ONLY; the payload-carried routing key re-homed as an ENV credential by label).
3. WAVE 6 rows: the ONE serial merge-writer alone records the merge-train rows, in train order: Skill 54 extensions plus content_sha restamp; the orchestrator skill directory; department, role, persona, and floor wiring; SOPs and doc updates; the PRD folder sync at project-prds/anthology-engine/ (scrubbed set); update.sh registration of the new skill directory plus the corrected skill count; the hash-content-manifest.py restamp of _index.json content_sha; the version bump to v19.0.0; the ANNOTATED tag v19.0.0 (git tag -a) created BEFORE the merge; the merge itself; and the fresh-clone verification (version, tag, skill count, content hashes, clean install via the standard updater, JSON-export-absence scan).
4. COMMAND CENTER repo rows (W3.1 Anthology department seeding and board contract; W3.2 the home-screen tiles code edit in src/app/page.tsx for Anthology, Podcast, and Interview; W3.3 the participant token route in the middleware.ts bypass list): those changes merge on THAT repo's OWN serial train under the same laws (canary-first, single writer, annotated tag, fresh-clone verify) and are CROSS-REFERENCED here with their own rows naming that repo.
5. WAVE 5 and WAVE 7 rows: canary drill outcomes on the operator box (evidence pointers, gate scores), the post-merge re-run from the MERGED repos, the FREEZE posture record (legacy n8n alive and untouched), and the HOLD record (fleet rollout repo-only until the operator OK).
6. PROHIBITIONS, scored: never a secret value (labels and locations only); never client personally identifiable information; never any content from the nine Downloads JSON exports, in whole, in part, or in fixture form (not even inside a diff summary); the platform name is Convert and Flow in every row; the Gate A column carries the passing score on feature rows and n/a on administrative rows (rotations, tags, syncs, holds).

---

## SECTION 1: PLANNING RECORD (pre-build, operator box)

| Date | Change | Repo/Path | Commit/Tag/PR | By |
|---|---|---|---|---|
| 2026-07-06 | PRD folder initialized (8 root files plus 5 design specs) at clawd/projects/Anthology-Engine/ | operator box (pre-repo) | n/a | Fable (master planner) |
| 2026-07-06 | SPEC.md authored (node-level technical specification: verified live evidence, skill layout, S0 to S9 contracts, 69-row node-to-skill mapping over all 221 legacy nodes, ledger schema plus transition matrix, model tiers, delivery, dashboard contract, T1 to T9, reuse/deprecation law); one-line pointer added to PRD.md document control | operator box (pre-repo); SPEC.md is operator-side full form, scrubbed for any repo copy | n/a | Fable (spec author) |
| 2026-07-06 | Revision 2 of the whole PRD package (PRD, SPEC, CHECKLIST, WAVE-PLAN, QC matrix, TODO, GOAL-COMMAND, all five design specs): locked decisions baked (orchestrator-calls-54 reaffirmed, Skill 53 untouched, only legacy n8n retires; producer ready-to-assemble trigger; auto-provisioned standard pipeline; existing Google service account and existing shared Drive root wired; BlackCEO Cloudflare dashboard); v19.0.0 target everywhere; Command Center kanban rollup plus home tiles (Anthology plus Skill 53); legacy bridge and migration stripped to a single optional appendix line; full identifier and PII scrub to LEGACY-* labels; script inventory with exit codes (SPEC 3.4); GOAL-COMMAND 3861 chars | operator box (pre-repo) | n/a | Fable (revision sub-agent) |
| 2026-07-06 | Revision 3 of the spine (PRD.md version 3.0, SPEC.md version 3.0, WAVE-PLAN.md version 3.0, GOAL-COMMAND.txt regenerated at 3887 characters, cap 3900 PASS): the producer experience relocated INSIDE the client's own Command Center per the verified Command Center repo read, superseding every standalone Mission Control and BlackCEO-hosted dashboard line (design/dashboard-design.md subordinated; PRD 3.15 to 3.17 and SPEC Section 11 govern): the build SEEDS an Anthology department via Skill 32 add-department.sh (equivalently POST /api/departments with create:true); cards ingest via mc_board.py to POST /api/tasks/ingest (HMAC plus Bearer, fail-soft); the board review column IS the chapter-approval queue and ONLY the independent QC scorer at or above 8.5 promotes review to done; the ready-to-assemble trigger and final sign-off ride status transitions plus a dedicated Assembly card; the Skill 53 never-seeded books-department defect is documented and not repeated. HOME-SCREEN TILES (Anthology, Podcast, Interview) become a code edit to the hardcoded tile array in command-center src/app/page.tsx on that repo's own serial train. NEW PARTICIPANT TOKEN PAGE: one token/PIN-scoped public route added to the middleware.ts bypass list, serving only the participant's open gate. PROMPT SOURCE OF RECORD moved to the nine Downloads JSON exports with Wave 0 ingestion (four largest prompts extracted verbatim and sha256-pinned, zero truncation; the formatter's 10 literal [UNCHANGED] placeholders restored from the CSV record HTML Book; the Write Chapter word band normalized to 2,000 to 3,500; Make.com variable syntax mapped to canonical field names). SECURITY LAW recorded: the nine exports carry live keys (by label: the cover-node Authorization-header key and the webhook-payload routing key), are NEVER committed, and both keys rotate at Wave 0 with the payload-carried key re-homed as an ENV credential by label. EXECUTION MODEL rewritten: only sub-agents build; up to 50 agents per wave, ONE wave at a time, staggered, never stacked; heavy parallelism on both the execution and QC sides; the 8.5 Gate A per feature (below, fix and re-QC; at or above, push); the incremental persistence law; GitHub canary-first, strictly serial single merge-writer, annotated tags only, v19.0.0, fresh-clone verify; two QC gates never conflated; DONE = every CHECKLIST Part C item (all 26) TRUE; fleet rollout HELD repo-only until the operator OK | operator box (pre-repo) | n/a | Fable (revision 3 spine sub-agents) |
| 2026-07-06 | CHANGE-LOG.md restructured for the build without touching any prior row: the ROW LAW stated (per-merged-feature appends by the same agent that passes Gate A and pushes; Wave 0, Wave 6 merge-writer, Command Center cross-reference, and Wave 5/7 row duties; prohibitions); Section 2, the v19.0.0 BUILD RECORD, added empty with its column contract, awaiting the first pushed feature | operator box (pre-repo); this file | n/a | Fable (doc sub-agent) |

---

## SECTION 2: v19.0.0 BUILD RECORD (append-only; one row per merged feature; empty until the build starts)

Column contract: Date = push date. Wave/Unit = the WAVE-PLAN.md unit id (W0.2, W1.7, W3.2, Wave 6 train step, and so on). Change = one merged feature or one administrative act (rotation by label, tag, sync, hold), stated in artifact terms. Gate A = the passing rubric score (at or above 8.5) on feature rows; n/a on administrative rows (Gate B, the content gate, is a runtime gate and is never recorded as a merge score). Repo = onboarding repo, Command Center repo, or operator box. Commit/Tag/PR = the commit hash, annotated tag, or pull request reference. By = the appending agent (the same agent that passed the gate and pushed; the Wave 6 rows by the single merge-writer only).

| Date | Wave/Unit | Change | Gate A | Repo | Commit/Tag/PR | By |
|---|---|---|---|---|---|---|
| 2026-07-06 | QC-PROTOCOL-AND-MATRIX.md revised to Version 3.0 (revision 3): QC-gated push and incremental persistence law spelled out; execution-model section added; Gate A riders gain zero-[UNCHANGED] and the JSON-export-absence scan; Command Center rows corrected to the seeded Anthology department, the three home tiles, and the participant token page; Gate B board surfacing rule added; runtime and build matrices re-keyed to the revision-3 waves and v19.0.0 | operator box (pre-repo), clawd/projects/Anthology-Engine/QC-PROTOCOL-AND-MATRIX.md | n/a | Fable (QC-doc revision sub-agent) |
