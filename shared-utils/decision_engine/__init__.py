"""JEV 1.1 decision engine (D02) — versioned decision envelope + bundle contracts.

Canonical home per spec section 2.2: openclaw-onboarding/shared-utils/decision_engine/.
Stdlib only. No network, no provider access, no state writes.
"""

__version__ = "1.0.0"
