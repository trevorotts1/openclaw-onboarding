"""JEV 1.1 decision-engine providers (spec section 3).

D04 owns ``typesafe_direct`` here. Each provider module is stdlib-only,
performs no network at import, and never touches credentials on disk.
"""
