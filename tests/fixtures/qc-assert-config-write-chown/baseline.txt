# qc-assert-config-write-chown baseline
# Generated by: bash scripts/qc-assert-config-write-chown.sh --write-baseline
# Format: one entry per line:
#   <repo-relative-file>::<normalized matched line><TAB><count>
# normalized = comment-stripped, whitespace-collapsed, trimmed.
# <count> = number of times this exact normalized line occurred in
#   this file when --write-baseline was run. Default mode FAILS a
#   key whose OBSERVED count exceeds this <count> (a new occurrence
#   of an already-baselined line), and treats a lower observed count
#   as partially-resolved debt (non-blocking, never fails).
# Comparison ignores line numbers so an entry survives unrelated
# line-number drift elsewhere in the file.
# Backward compatibility: a line with no <TAB><count> field (written
#   before this count-aware format existed) is read as count=1.
# Regenerate after fixing debt: bash scripts/qc-assert-config-write-chown.sh --write-baseline
install.sh::json.dump(cfg, open(p, 'w'), indent=2)	1
install.sh::json.dump(d, open(p, 'w'), indent=2)	1
install.sh::note "ensure-heartbeat-defaults.sh not in bundle — skipping (set manually: openclaw config set agents.defaults.heartbeat.every 6h)"	1
install.sh::open(p, "w", encoding="utf-8").write(new)	1
install.sh::openclaw config set agents.defaults.workspace "$WORKSPACE_DIR" 2>/dev/null || true	1
install.sh::p.write_text(json.dumps(cfg, indent=2) + "\n")	1
scripts/ensure-heartbeat-defaults.sh::echo " ⚠ Cannot add per-agent heartbeat block (no openclaw CLI / no python3) — manual: openclaw config set 'agents.list[$PRIMARY_IDX].heartbeat.every' $HB_EVERY" >&2	1
scripts/ensure-heartbeat-defaults.sh::echo " ⚠ Could not set agents.defaults.heartbeat.every — manual: openclaw config set agents.defaults.heartbeat.every 6h" >&2	1
scripts/ensure-heartbeat-defaults.sh::echo " ⚠ openclaw not on PATH — interval default not set; run: openclaw config set agents.defaults.heartbeat.every 6h" >&2	1
scripts/ensure-heartbeat-defaults.sh::if openclaw config set "agents.list[$PRIMARY_IDX].heartbeat.every" "$HB_EVERY" 2>>"$LOG_FILE"; then	1
scripts/ensure-heartbeat-defaults.sh::openclaw config set "agents.defaults.heartbeat.model" "$HB_MODEL" 2>>"$LOG_FILE" || true	1
scripts/ensure-heartbeat-defaults.sh::openclaw config set "agents.list[$PRIMARY_IDX].heartbeat.model" "$HB_MODEL" 2>>"$LOG_FILE" || true	1
scripts/ensure-heartbeat-defaults.sh::openclaw config set agents.defaults.heartbeat.every 6h 2>>"$LOG_FILE" || _iv=0	1
scripts/ensure-heartbeat-defaults.sh::openclaw config set agents.defaults.heartbeat.maxTokens 2000 2>>"$LOG_FILE" || true	1
scripts/ghl-mcp-autostart.sh::openclaw config set env.vars.GHL_COMMUNITY_MCP_URL "$_ghl_want" >/dev/null 2>&1 || true	1
scripts/telegram-offset-healthcheck.sh::BOT_TOKEN=$(jq -r '.channels.telegram.botToken // .channels.telegram.token // empty' "$CONFIG_FILE" 2>/dev/null)	1
scripts/test-ceo-tool-gate.sh::|| _bad "B: L5 NOT idempotent — re-run changed openclaw.json ($_B_BEFORE -> $_B_AFTER)"	1
scripts/test-ceo-tool-gate.sh::cat > "$HOME/.openclaw/openclaw.json" <<'JSON'	9
scripts/test-ceo-tool-gate.sh::cat > "$HOME/.openclaw/openclaw.json" <<JSON	1
scripts/test-fleet-refresh.sh::cat > "$OC_ROOT/openclaw.json" <<JSON	1
scripts/test-fleet-refresh.sh::cfg.write_text(json.dumps({	2
scripts/test-smoke-provider-internal-channel.sh::cat > "$TMP/openclaw.json" <<'JSON'	1
scripts/test-updater-traps-1-and-3.sh::printf '{"agents":{"a":{"workspace":"%s/.clawdbot/ws"}}}\n' "$r" > "$r/.openclaw/openclaw.json"	1
scripts/test-updater-traps-1-and-3.sh::printf '{"agents":{}}\n' > "$r/.openclaw/openclaw.json"	3
update-skills.sh::&& openclaw config set agents.defaults.memorySearch.model gemini-embedding-2 >>"$LOG_FILE" 2>&1; then	1
update-skills.sh::&& openclaw config set agents.defaults.memorySearch.model text-embedding-3-small >>"$LOG_FILE" 2>&1; then	1
update-skills.sh::echo " 'openclaw config set tools.loopDetection.enabled true' once on a supported build."	1
update-skills.sh::elif openclaw config set agents.defaults.memorySearch.provider gemini >>"$LOG_FILE" 2>&1 \	1
update-skills.sh::elif openclaw config set agents.defaults.memorySearch.provider openai >>"$LOG_FILE" 2>&1 \	1
update-skills.sh::elif openclaw config set models.providers.google.models \	1
update-skills.sh::elif openclaw config set plugins.entries.memory-core.config.dreaming \	1
update-skills.sh::elif openclaw config set plugins.entries.memory-core.enabled true >>"$LOG_FILE" 2>&1; then	1
update-skills.sh::elif openclaw config set plugins.entries.memory-lancedb.enabled false >>"$LOG_FILE" 2>&1; then	1
update-skills.sh::elif openclaw config set tools.loopDetection.enabled true >>"$LOG_FILE" 2>&1; then	1
update-skills.sh::openclaw config set "$_QMD_KEY" false >>"$LOG_FILE" 2>&1 \	1
update-skills.sh::openclaw config set env.vars.GHL_COMMUNITY_MCP_URL "http://localhost:${GHL_MCP_PORT}" >> "$LOG_FILE" 2>&1 || true	1
