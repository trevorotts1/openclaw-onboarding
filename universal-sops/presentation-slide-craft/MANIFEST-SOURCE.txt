content_sha256=8d9cbbe6efd54330f56384e7f9b9a7966a82ca9d8cd936bbd4f7e6237b360d82
generated=2026-08-27T00:00:00Z
source=PIPELINE-MANIFEST.json (v51, unchanged version, Ticket 7 fix/presentation-intake-gate-20260827 (L-11): P9.6-WEBINAR-VIDEO's executor changed from {"kind":"script","cmd":"bash scripts/presentation-canonical-entry.sh --resume --run-dir {run_dir}"} to {"kind":"script","cmd":"python3 scripts/build_webinar_video.py --run-dir {run_dir}"} — every other script-kind phase in this manifest calls its own builder directly; P9.6 (and its still-unfixed twin P8.25-WORKBOOK) were the only two wired to re-invoke the top-level canonical-entry.sh launcher instead. That recursive call spawns a second `python3 presentation_job.py --run --run-dir {run_dir}` while the dispatching Engine's own process is still inside `with RunLock(run_dir):` for the full `engine.run()` call (presentation_job/__main__.py:582), so the inner process always dies immediately with EXIT_LOCK_HELD (presentation_job/state.py's RunLock.__enter__ uses a non-blocking flock; dispatcher.py's own header comment documents this exact hazard) — P9.6 could never complete via its old executor.cmd. The direct call is safe: OC_DECK_ENTRY_NONCE is minted once by the ORIGINAL top-level canonical-entry.sh invocation and inherited by the whole descendant process tree (engine -> this phase's subprocess, no env= override anywhere in that chain), so build_webinar_video.py's own _verify_entry_nonce() still passes exactly as it does for every sibling script phase, and a hand-rolled direct invocation of build_webinar_video.py still fails closed (AF-CANONICAL-RENDER-BYPASS) exactly as before. No phase added/removed/renamed, phase count stays 40, AF codes unchanged, content_sha256 restamped against the merged content)
