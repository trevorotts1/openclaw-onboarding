content_sha256=29bbe74458609e0e04b7df0ea653a5335da223a1e7c9b65ab4b4654a5ecc0943
generated=2026-08-27T00:00:00Z
source=PIPELINE-MANIFEST.json (v51, unchanged version, Ticket 7 follow-up fix/presentation-intake-gate-20260827 (L-11 twin): P8.25-WORKBOOK's executor changed from {"kind":"script","cmd":"bash scripts/presentation-canonical-entry.sh --resume --run-dir {run_dir}"} to {"kind":"script","cmd":"python3 scripts/workbook_builder.py --run-dir {run_dir}"} — the same RunLock self-collision fixed for P9.6-WEBINAR-VIDEO earlier in this branch (see this file's prior entry) applied identically to P8.25-WORKBOOK, which was the other of the manifest's two script-kind phases wired to re-invoke the top-level canonical-entry.sh launcher instead of its own builder. Verified workbook_builder.py's CLI contract before wiring it directly: --run-dir is its required argument (or --selftest), and its own _verify_entry_nonce() (workbook_builder.py:1262-1271) is a near-exact structural mirror of build_webinar_video.py's — same OC_DECK_ENTRY_NONCE env read, same --no-upload smoke-test exemption, same AF-CANONICAL-RENDER-BYPASS fail-closed message — so the same env-inheritance argument applies unchanged: the nonce is minted once by the ORIGINAL top-level canonical-entry.sh invocation and inherited by the whole descendant process tree (no env= override anywhere in phases.py/heal.py/process_reaper.run_with_cleanup's dispatch chain), so a genuinely hand-rolled direct invocation still fails closed exactly as before. Zero phases in this manifest now point at presentation-canonical-entry.sh as a per-phase executor (verified by scanning every phase's executor.cmd). No phase added/removed/renamed, phase count stays 40, AF codes unchanged, content_sha256 restamped against the merged content)
